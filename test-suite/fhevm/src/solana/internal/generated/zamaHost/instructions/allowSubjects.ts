/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findHostConfigPda } from '../pdas/index.js';
import { ZAMA_HOST_PROGRAM_ADDRESS } from '../programAddress.js';

export const ALLOW_SUBJECTS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([186, 205, 31, 20, 183, 17, 5, 26]);

export function getAllowSubjectsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ALLOW_SUBJECTS_DISCRIMINATOR);
}

export type AllowSubjectsInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountEncryptedValue extends string | AccountMeta<string> = string,
  TAccountHostConfig extends string | AccountMeta<string> = string,
  TAccountDenySubjectRecord extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountEncryptedValue extends string ? WritableAccount<TAccountEncryptedValue> : TAccountEncryptedValue,
      TAccountHostConfig extends string ? ReadonlyAccount<TAccountHostConfig> : TAccountHostConfig,
      TAccountDenySubjectRecord extends string ? ReadonlyAccount<TAccountDenySubjectRecord> : TAccountDenySubjectRecord,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AllowSubjectsInstructionData = {
  discriminator: ReadonlyUint8Array;
  subjects: Array<Address>;
};

export type AllowSubjectsInstructionDataArgs = { subjects: Array<Address> };

export function getAllowSubjectsInstructionDataEncoder(): Encoder<AllowSubjectsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['subjects', getArrayEncoder(getAddressEncoder())],
    ]),
    (value) => ({ ...value, discriminator: ALLOW_SUBJECTS_DISCRIMINATOR }),
  );
}

export function getAllowSubjectsInstructionDataDecoder(): Decoder<AllowSubjectsInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['subjects', getArrayDecoder(getAddressDecoder())],
  ]);
}

export function getAllowSubjectsInstructionDataCodec(): Codec<
  AllowSubjectsInstructionDataArgs,
  AllowSubjectsInstructionData
> {
  return combineCodec(getAllowSubjectsInstructionDataEncoder(), getAllowSubjectsInstructionDataDecoder());
}

export type AllowSubjectsAsyncInput<
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountEncryptedValue extends string = string,
  TAccountHostConfig extends string = string,
  TAccountDenySubjectRecord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays for the account's growth, if any. */
  payer: TransactionSigner<TAccountPayer>;
  /**
   * Must equal `EncryptedValue.encrypted_value_account_authority` (same gate as
   * `fhe_execute` persistent create/update). Subjects alone cannot mutate
   * membership — apps that store a PDA in `encrypted_value_account_authority`
   * rotate auditors by CPI + `invoke_signed` as that PDA.
   */
  authority: TransactionSigner<TAccountAuthority>;
  encryptedValue: Address<TAccountEncryptedValue>;
  hostConfig?: Address<TAccountHostConfig>;
  denySubjectRecord?: Address<TAccountDenySubjectRecord>;
  systemProgram?: Address<TAccountSystemProgram>;
  subjects: AllowSubjectsInstructionDataArgs['subjects'];
};

export async function getAllowSubjectsInstructionAsync<
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountEncryptedValue extends string,
  TAccountHostConfig extends string,
  TAccountDenySubjectRecord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: AllowSubjectsAsyncInput<
    TAccountPayer,
    TAccountAuthority,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountDenySubjectRecord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AllowSubjectsInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountDenySubjectRecord,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    encryptedValue: { value: input.encryptedValue ?? null, isWritable: true },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    denySubjectRecord: {
      value: input.denySubjectRecord ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hostConfig.value) {
    accounts.hostConfig.value = await findHostConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('encryptedValue', accounts.encryptedValue),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('denySubjectRecord', accounts.denySubjectRecord),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getAllowSubjectsInstructionDataEncoder().encode(args as AllowSubjectsInstructionDataArgs),
    programAddress,
  } as AllowSubjectsInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountDenySubjectRecord,
    TAccountSystemProgram
  >);
}

export type AllowSubjectsInput<
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountEncryptedValue extends string = string,
  TAccountHostConfig extends string = string,
  TAccountDenySubjectRecord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays for the account's growth, if any. */
  payer: TransactionSigner<TAccountPayer>;
  /**
   * Must equal `EncryptedValue.encrypted_value_account_authority` (same gate as
   * `fhe_execute` persistent create/update). Subjects alone cannot mutate
   * membership — apps that store a PDA in `encrypted_value_account_authority`
   * rotate auditors by CPI + `invoke_signed` as that PDA.
   */
  authority: TransactionSigner<TAccountAuthority>;
  encryptedValue: Address<TAccountEncryptedValue>;
  hostConfig: Address<TAccountHostConfig>;
  denySubjectRecord?: Address<TAccountDenySubjectRecord>;
  systemProgram?: Address<TAccountSystemProgram>;
  subjects: AllowSubjectsInstructionDataArgs['subjects'];
};

export function getAllowSubjectsInstruction<
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountEncryptedValue extends string,
  TAccountHostConfig extends string,
  TAccountDenySubjectRecord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: AllowSubjectsInput<
    TAccountPayer,
    TAccountAuthority,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountDenySubjectRecord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AllowSubjectsInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountAuthority,
  TAccountEncryptedValue,
  TAccountHostConfig,
  TAccountDenySubjectRecord,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    encryptedValue: { value: input.encryptedValue ?? null, isWritable: true },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    denySubjectRecord: {
      value: input.denySubjectRecord ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('encryptedValue', accounts.encryptedValue),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('denySubjectRecord', accounts.denySubjectRecord),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getAllowSubjectsInstructionDataEncoder().encode(args as AllowSubjectsInstructionDataArgs),
    programAddress,
  } as AllowSubjectsInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountDenySubjectRecord,
    TAccountSystemProgram
  >);
}

export type ParsedAllowSubjectsInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays for the account's growth, if any. */
    payer: TAccountMetas[0];
    /**
     * Must equal `EncryptedValue.encrypted_value_account_authority` (same gate as
     * `fhe_execute` persistent create/update). Subjects alone cannot mutate
     * membership — apps that store a PDA in `encrypted_value_account_authority`
     * rotate auditors by CPI + `invoke_signed` as that PDA.
     */
    authority: TAccountMetas[1];
    encryptedValue: TAccountMetas[2];
    hostConfig: TAccountMetas[3];
    denySubjectRecord?: TAccountMetas[4] | undefined;
    systemProgram: TAccountMetas[5];
  };
  data: AllowSubjectsInstructionData;
};

export function parseAllowSubjectsInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedAllowSubjectsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 6,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === ZAMA_HOST_PROGRAM_ADDRESS ? undefined : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      authority: getNextAccount(),
      encryptedValue: getNextAccount(),
      hostConfig: getNextAccount(),
      denySubjectRecord: getNextOptionalAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAllowSubjectsInstructionDataDecoder().decode(instruction.data),
  };
}
