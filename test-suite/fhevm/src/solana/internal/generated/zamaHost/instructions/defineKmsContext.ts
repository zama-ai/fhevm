/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findHostConfigPda, findKmsContextPda } from '../pdas/index.js';
import { ZAMA_HOST_PROGRAM_ADDRESS } from '../programAddress.js';
import {
  getKmsThresholdsDecoder,
  getKmsThresholdsEncoder,
  type KmsThresholds,
  type KmsThresholdsArgs,
} from '../types/index.js';

export const DEFINE_KMS_CONTEXT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  102, 233, 87, 23, 174, 186, 233, 87,
]);

export function getDefineKmsContextDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DEFINE_KMS_CONTEXT_DISCRIMINATOR);
}

export type DefineKmsContextInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountHostConfig extends string | AccountMeta<string> = string,
  TAccountKmsContext extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? WritableSignerAccount<TAccountAdmin> & AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountHostConfig extends string ? WritableAccount<TAccountHostConfig> : TAccountHostConfig,
      TAccountKmsContext extends string ? WritableAccount<TAccountKmsContext> : TAccountKmsContext,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      TAccountEventAuthority extends string ? ReadonlyAccount<TAccountEventAuthority> : TAccountEventAuthority,
      TAccountProgram extends string ? ReadonlyAccount<TAccountProgram> : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DefineKmsContextInstructionData = {
  discriminator: ReadonlyUint8Array;
  contextId: bigint;
  signers: Array<ReadonlyUint8Array>;
  thresholds: KmsThresholds;
};

export type DefineKmsContextInstructionDataArgs = {
  contextId: number | bigint;
  signers: Array<ReadonlyUint8Array>;
  thresholds: KmsThresholdsArgs;
};

export function getDefineKmsContextInstructionDataEncoder(): Encoder<DefineKmsContextInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['contextId', getU64Encoder()],
      ['signers', getArrayEncoder(fixEncoderSize(getBytesEncoder(), 20))],
      ['thresholds', getKmsThresholdsEncoder()],
    ]),
    (value) => ({ ...value, discriminator: DEFINE_KMS_CONTEXT_DISCRIMINATOR }),
  );
}

export function getDefineKmsContextInstructionDataDecoder(): Decoder<DefineKmsContextInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['contextId', getU64Decoder()],
    ['signers', getArrayDecoder(fixDecoderSize(getBytesDecoder(), 20))],
    ['thresholds', getKmsThresholdsDecoder()],
  ]);
}

export function getDefineKmsContextInstructionDataCodec(): Codec<
  DefineKmsContextInstructionDataArgs,
  DefineKmsContextInstructionData
> {
  return combineCodec(getDefineKmsContextInstructionDataEncoder(), getDefineKmsContextInstructionDataDecoder());
}

export type DefineKmsContextAsyncInput<
  TAccountAdmin extends string = string,
  TAccountHostConfig extends string = string,
  TAccountKmsContext extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** Configured host admin and rent payer for the context account. */
  admin: TransactionSigner<TAccountAdmin>;
  /** Singleton config PDA; its `current_kms_context_id` is advanced to `context_id`. */
  hostConfig?: Address<TAccountHostConfig>;
  /** KMS context PDA created for `context_id`. */
  kmsContext?: Address<TAccountKmsContext>;
  /** System program used for account creation. */
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  contextId: DefineKmsContextInstructionDataArgs['contextId'];
  signers: DefineKmsContextInstructionDataArgs['signers'];
  thresholds: DefineKmsContextInstructionDataArgs['thresholds'];
};

export async function getDefineKmsContextInstructionAsync<
  TAccountAdmin extends string,
  TAccountHostConfig extends string,
  TAccountKmsContext extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: DefineKmsContextAsyncInput<
    TAccountAdmin,
    TAccountHostConfig,
    TAccountKmsContext,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DefineKmsContextInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountHostConfig,
    TAccountKmsContext,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    hostConfig: { value: input.hostConfig ?? null, isWritable: true },
    kmsContext: { value: input.kmsContext ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hostConfig.value) {
    accounts.hostConfig.value = await findHostConfigPda();
  }
  if (!accounts.kmsContext.value) {
    accounts.kmsContext.value = await findKmsContextPda({
      contextId: getNonNullResolvedInstructionInput('contextId', args.contextId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('admin', accounts.admin),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('kmsContext', accounts.kmsContext),
      getAccountMeta('systemProgram', accounts.systemProgram),
      getAccountMeta('eventAuthority', accounts.eventAuthority),
      getAccountMeta('program', accounts.program),
    ],
    data: getDefineKmsContextInstructionDataEncoder().encode(args as DefineKmsContextInstructionDataArgs),
    programAddress,
  } as DefineKmsContextInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountHostConfig,
    TAccountKmsContext,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type DefineKmsContextInput<
  TAccountAdmin extends string = string,
  TAccountHostConfig extends string = string,
  TAccountKmsContext extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** Configured host admin and rent payer for the context account. */
  admin: TransactionSigner<TAccountAdmin>;
  /** Singleton config PDA; its `current_kms_context_id` is advanced to `context_id`. */
  hostConfig: Address<TAccountHostConfig>;
  /** KMS context PDA created for `context_id`. */
  kmsContext: Address<TAccountKmsContext>;
  /** System program used for account creation. */
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  contextId: DefineKmsContextInstructionDataArgs['contextId'];
  signers: DefineKmsContextInstructionDataArgs['signers'];
  thresholds: DefineKmsContextInstructionDataArgs['thresholds'];
};

export function getDefineKmsContextInstruction<
  TAccountAdmin extends string,
  TAccountHostConfig extends string,
  TAccountKmsContext extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: DefineKmsContextInput<
    TAccountAdmin,
    TAccountHostConfig,
    TAccountKmsContext,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DefineKmsContextInstruction<
  TProgramAddress,
  TAccountAdmin,
  TAccountHostConfig,
  TAccountKmsContext,
  TAccountSystemProgram,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: true },
    hostConfig: { value: input.hostConfig ?? null, isWritable: true },
    kmsContext: { value: input.kmsContext ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('admin', accounts.admin),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('kmsContext', accounts.kmsContext),
      getAccountMeta('systemProgram', accounts.systemProgram),
      getAccountMeta('eventAuthority', accounts.eventAuthority),
      getAccountMeta('program', accounts.program),
    ],
    data: getDefineKmsContextInstructionDataEncoder().encode(args as DefineKmsContextInstructionDataArgs),
    programAddress,
  } as DefineKmsContextInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountHostConfig,
    TAccountKmsContext,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedDefineKmsContextInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Configured host admin and rent payer for the context account. */
    admin: TAccountMetas[0];
    /** Singleton config PDA; its `current_kms_context_id` is advanced to `context_id`. */
    hostConfig: TAccountMetas[1];
    /** KMS context PDA created for `context_id`. */
    kmsContext: TAccountMetas[2];
    /** System program used for account creation. */
    systemProgram: TAccountMetas[3];
    eventAuthority: TAccountMetas[4];
    program: TAccountMetas[5];
  };
  data: DefineKmsContextInstructionData;
};

export function parseDefineKmsContextInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedDefineKmsContextInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 6,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      admin: getNextAccount(),
      hostConfig: getNextAccount(),
      kmsContext: getNextAccount(),
      systemProgram: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getDefineKmsContextInstructionDataDecoder().decode(instruction.data),
  };
}
