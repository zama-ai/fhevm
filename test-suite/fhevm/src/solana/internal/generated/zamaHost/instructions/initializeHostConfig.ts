/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findHostConfigPda } from '../pdas/index.js';
import { ZAMA_HOST_PROGRAM_ADDRESS } from '../programAddress.js';

export const INITIALIZE_HOST_CONFIG_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  37, 11, 53, 51, 140, 246, 55, 96,
]);

export function getInitializeHostConfigDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(INITIALIZE_HOST_CONFIG_DISCRIMINATOR);
}

export type InitializeHostConfigInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TAccountHostConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> & AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountHostConfig extends string ? WritableAccount<TAccountHostConfig> : TAccountHostConfig,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      TAccountEventAuthority extends string ? ReadonlyAccount<TAccountEventAuthority> : TAccountEventAuthority,
      TAccountProgram extends string ? ReadonlyAccount<TAccountProgram> : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeHostConfigInstructionData = {
  discriminator: ReadonlyUint8Array;
  /** Host-chain id encoded into newly derived handles. */
  chainId: bigint;
  /** EVM gateway chain id used in the coprocessor/KMS EIP-712 domain separators. */
  gatewayChainId: bigint;
  /** EVM `InputVerification` contract address (EIP-712 verifying contract). */
  inputVerificationContract: ReadonlyUint8Array;
  /**
   * Registered coprocessor EVM signer set for input attestations (EVM `InputVerifier`
   * parity). Must be non-empty, distinct, and free of the zero address; bounded by
   * [`HostConfig::MAX_COPROCESSOR_SIGNERS`].
   */
  coprocessorSigners: Array<ReadonlyUint8Array>;
  /**
   * Minimum distinct valid signatures (n-of-m) required over an input attestation;
   * `1 <= coprocessor_threshold <= coprocessor_signers.len()`.
   */
  coprocessorThreshold: number;
  /** EVM `Decryption` contract address (EIP-712 verifying contract for KMS certs). */
  decryptionContract: ReadonlyUint8Array;
  /** Whether persistent grants must include a deny-list witness. */
  grantDenyListEnabled: boolean;
};

export type InitializeHostConfigInstructionDataArgs = {
  /** Host-chain id encoded into newly derived handles. */
  chainId: number | bigint;
  /** EVM gateway chain id used in the coprocessor/KMS EIP-712 domain separators. */
  gatewayChainId: number | bigint;
  /** EVM `InputVerification` contract address (EIP-712 verifying contract). */
  inputVerificationContract: ReadonlyUint8Array;
  /**
   * Registered coprocessor EVM signer set for input attestations (EVM `InputVerifier`
   * parity). Must be non-empty, distinct, and free of the zero address; bounded by
   * [`HostConfig::MAX_COPROCESSOR_SIGNERS`].
   */
  coprocessorSigners: Array<ReadonlyUint8Array>;
  /**
   * Minimum distinct valid signatures (n-of-m) required over an input attestation;
   * `1 <= coprocessor_threshold <= coprocessor_signers.len()`.
   */
  coprocessorThreshold: number;
  /** EVM `Decryption` contract address (EIP-712 verifying contract for KMS certs). */
  decryptionContract: ReadonlyUint8Array;
  /** Whether persistent grants must include a deny-list witness. */
  grantDenyListEnabled: boolean;
};

export function getInitializeHostConfigInstructionDataEncoder(): Encoder<InitializeHostConfigInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['chainId', getU64Encoder()],
      ['gatewayChainId', getU64Encoder()],
      ['inputVerificationContract', fixEncoderSize(getBytesEncoder(), 20)],
      ['coprocessorSigners', getArrayEncoder(fixEncoderSize(getBytesEncoder(), 20))],
      ['coprocessorThreshold', getU8Encoder()],
      ['decryptionContract', fixEncoderSize(getBytesEncoder(), 20)],
      ['grantDenyListEnabled', getBooleanEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_HOST_CONFIG_DISCRIMINATOR,
    }),
  );
}

export function getInitializeHostConfigInstructionDataDecoder(): Decoder<InitializeHostConfigInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['chainId', getU64Decoder()],
    ['gatewayChainId', getU64Decoder()],
    ['inputVerificationContract', fixDecoderSize(getBytesDecoder(), 20)],
    ['coprocessorSigners', getArrayDecoder(fixDecoderSize(getBytesDecoder(), 20))],
    ['coprocessorThreshold', getU8Decoder()],
    ['decryptionContract', fixDecoderSize(getBytesDecoder(), 20)],
    ['grantDenyListEnabled', getBooleanDecoder()],
  ]);
}

export function getInitializeHostConfigInstructionDataCodec(): Codec<
  InitializeHostConfigInstructionDataArgs,
  InitializeHostConfigInstructionData
> {
  return combineCodec(getInitializeHostConfigInstructionDataEncoder(), getInitializeHostConfigInstructionDataDecoder());
}

export type InitializeHostConfigAsyncInput<
  TAccountPayer extends string = string,
  TAccountAdmin extends string = string,
  TAccountHostConfig extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** Pays rent for the config account. */
  payer: TransactionSigner<TAccountPayer>;
  /** Initial admin stored in the config. */
  admin: TransactionSigner<TAccountAdmin>;
  /** Singleton config PDA. */
  hostConfig?: Address<TAccountHostConfig>;
  /** System program used for account creation. */
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  chainId: InitializeHostConfigInstructionDataArgs['chainId'];
  gatewayChainId: InitializeHostConfigInstructionDataArgs['gatewayChainId'];
  inputVerificationContract: InitializeHostConfigInstructionDataArgs['inputVerificationContract'];
  coprocessorSigners: InitializeHostConfigInstructionDataArgs['coprocessorSigners'];
  coprocessorThreshold: InitializeHostConfigInstructionDataArgs['coprocessorThreshold'];
  decryptionContract: InitializeHostConfigInstructionDataArgs['decryptionContract'];
  grantDenyListEnabled: InitializeHostConfigInstructionDataArgs['grantDenyListEnabled'];
};

export async function getInitializeHostConfigInstructionAsync<
  TAccountPayer extends string,
  TAccountAdmin extends string,
  TAccountHostConfig extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: InitializeHostConfigAsyncInput<
    TAccountPayer,
    TAccountAdmin,
    TAccountHostConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeHostConfigInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAdmin,
    TAccountHostConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
    hostConfig: { value: input.hostConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hostConfig.value) {
    accounts.hostConfig.value = await findHostConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('admin', accounts.admin),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('systemProgram', accounts.systemProgram),
      getAccountMeta('eventAuthority', accounts.eventAuthority),
      getAccountMeta('program', accounts.program),
    ],
    data: getInitializeHostConfigInstructionDataEncoder().encode(args as InitializeHostConfigInstructionDataArgs),
    programAddress,
  } as InitializeHostConfigInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAdmin,
    TAccountHostConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type InitializeHostConfigInput<
  TAccountPayer extends string = string,
  TAccountAdmin extends string = string,
  TAccountHostConfig extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** Pays rent for the config account. */
  payer: TransactionSigner<TAccountPayer>;
  /** Initial admin stored in the config. */
  admin: TransactionSigner<TAccountAdmin>;
  /** Singleton config PDA. */
  hostConfig: Address<TAccountHostConfig>;
  /** System program used for account creation. */
  systemProgram?: Address<TAccountSystemProgram>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  chainId: InitializeHostConfigInstructionDataArgs['chainId'];
  gatewayChainId: InitializeHostConfigInstructionDataArgs['gatewayChainId'];
  inputVerificationContract: InitializeHostConfigInstructionDataArgs['inputVerificationContract'];
  coprocessorSigners: InitializeHostConfigInstructionDataArgs['coprocessorSigners'];
  coprocessorThreshold: InitializeHostConfigInstructionDataArgs['coprocessorThreshold'];
  decryptionContract: InitializeHostConfigInstructionDataArgs['decryptionContract'];
  grantDenyListEnabled: InitializeHostConfigInstructionDataArgs['grantDenyListEnabled'];
};

export function getInitializeHostConfigInstruction<
  TAccountPayer extends string,
  TAccountAdmin extends string,
  TAccountHostConfig extends string,
  TAccountSystemProgram extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: InitializeHostConfigInput<
    TAccountPayer,
    TAccountAdmin,
    TAccountHostConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeHostConfigInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountAdmin,
  TAccountHostConfig,
  TAccountSystemProgram,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
    hostConfig: { value: input.hostConfig ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('admin', accounts.admin),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('systemProgram', accounts.systemProgram),
      getAccountMeta('eventAuthority', accounts.eventAuthority),
      getAccountMeta('program', accounts.program),
    ],
    data: getInitializeHostConfigInstructionDataEncoder().encode(args as InitializeHostConfigInstructionDataArgs),
    programAddress,
  } as InitializeHostConfigInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAdmin,
    TAccountHostConfig,
    TAccountSystemProgram,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedInitializeHostConfigInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays rent for the config account. */
    payer: TAccountMetas[0];
    /** Initial admin stored in the config. */
    admin: TAccountMetas[1];
    /** Singleton config PDA. */
    hostConfig: TAccountMetas[2];
    /** System program used for account creation. */
    systemProgram: TAccountMetas[3];
    eventAuthority: TAccountMetas[4];
    program: TAccountMetas[5];
  };
  data: InitializeHostConfigInstructionData;
};

export function parseInitializeHostConfigInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeHostConfigInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 6,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      admin: getNextAccount(),
      hostConfig: getNextAccount(),
      systemProgram: getNextAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getInitializeHostConfigInstructionDataDecoder().decode(instruction.data),
  };
}
