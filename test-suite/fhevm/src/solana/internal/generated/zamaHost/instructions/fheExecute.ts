/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findHostConfigPda } from '../pdas/index.js';
import { ZAMA_HOST_PROGRAM_ADDRESS } from '../programAddress.js';
import {
  getFheExecuteStepDecoder,
  getFheExecuteStepEncoder,
  type FheExecuteStep,
  type FheExecuteStepArgs,
} from '../types/index.js';

export const FHE_EXECUTE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([96, 253, 7, 139, 8, 187, 108, 43]);

export function getFheExecuteDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(FHE_EXECUTE_DISCRIMINATOR);
}

export type FheExecuteInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountComputeSubject extends string | AccountMeta<string> = string,
  TAccountEncryptedValueAccountAuthority extends string | AccountMeta<string> = string,
  TAccountHostConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TAccountHcuBlockMeter extends string | AccountMeta<string> = string,
  TAccountHcuTrustedAppRecord extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountComputeSubject extends string
        ? ReadonlySignerAccount<TAccountComputeSubject> & AccountSignerMeta<TAccountComputeSubject>
        : TAccountComputeSubject,
      TAccountEncryptedValueAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountEncryptedValueAccountAuthority> &
            AccountSignerMeta<TAccountEncryptedValueAccountAuthority>
        : TAccountEncryptedValueAccountAuthority,
      TAccountHostConfig extends string ? ReadonlyAccount<TAccountHostConfig> : TAccountHostConfig,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      TAccountHcuBlockMeter extends string ? WritableAccount<TAccountHcuBlockMeter> : TAccountHcuBlockMeter,
      TAccountHcuTrustedAppRecord extends string
        ? ReadonlyAccount<TAccountHcuTrustedAppRecord>
        : TAccountHcuTrustedAppRecord,
      TAccountEventAuthority extends string ? ReadonlyAccount<TAccountEventAuthority> : TAccountEventAuthority,
      TAccountProgram extends string ? ReadonlyAccount<TAccountProgram> : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FheExecuteInstructionData = {
  discriminator: ReadonlyUint8Array;
  /**
   * Declared `remaining_accounts` length, asserted equal to the actual list. Carried in
   * instruction data so stateless indexers can validate account references without the
   * transaction envelope (DD-033 self-description).
   */
  accountCount: number;
  /**
   * Interned 32-byte constant dictionary: operand handles, scalar values, ACL domain keys,
   * encrypted value account authority keys, encrypted value labels, and output subjects. Steps
   * reference entries by `u8` index, so a value repeated across steps is paid for once (the
   * compiled-message / constant-dictionary encoding; fhevm-internal#1853 W7).
   *
   * The entries stay raw `[u8; 32]` on purpose, and must not become an enum of typed
   * variants. Interning is what makes the encoding small, and it only works across roles:
   * the same 32 bytes can be an operand handle in one step and an output subject in
   * another, and both steps then share one entry. A typed dictionary would need one entry
   * per role, which grows the packet and buys nothing — the step that reads an index
   * already knows which role it is asking for, and `dictionary_key` /
   * `dictionary_bytes` are where that reading happens. Types belong at the ends of the
   * wire, not in the middle of it (fhevm-internal#1859 §2).
   */
  dictionary: Array<ReadonlyUint8Array>;
  /**
   * Ordered step list. Each `EarlierStep` operand may only reference an output
   * produced by an earlier index in this vector.
   */
  steps: Array<FheExecuteStep>;
};

export type FheExecuteInstructionDataArgs = {
  /**
   * Declared `remaining_accounts` length, asserted equal to the actual list. Carried in
   * instruction data so stateless indexers can validate account references without the
   * transaction envelope (DD-033 self-description).
   */
  accountCount: number;
  /**
   * Interned 32-byte constant dictionary: operand handles, scalar values, ACL domain keys,
   * encrypted value account authority keys, encrypted value labels, and output subjects. Steps
   * reference entries by `u8` index, so a value repeated across steps is paid for once (the
   * compiled-message / constant-dictionary encoding; fhevm-internal#1853 W7).
   *
   * The entries stay raw `[u8; 32]` on purpose, and must not become an enum of typed
   * variants. Interning is what makes the encoding small, and it only works across roles:
   * the same 32 bytes can be an operand handle in one step and an output subject in
   * another, and both steps then share one entry. A typed dictionary would need one entry
   * per role, which grows the packet and buys nothing — the step that reads an index
   * already knows which role it is asking for, and `dictionary_key` /
   * `dictionary_bytes` are where that reading happens. Types belong at the ends of the
   * wire, not in the middle of it (fhevm-internal#1859 §2).
   */
  dictionary: Array<ReadonlyUint8Array>;
  /**
   * Ordered step list. Each `EarlierStep` operand may only reference an output
   * produced by an earlier index in this vector.
   */
  steps: Array<FheExecuteStepArgs>;
};

export function getFheExecuteInstructionDataEncoder(): Encoder<FheExecuteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['accountCount', getU8Encoder()],
      ['dictionary', getArrayEncoder(fixEncoderSize(getBytesEncoder(), 32))],
      ['steps', getArrayEncoder(getFheExecuteStepEncoder())],
    ]),
    (value) => ({ ...value, discriminator: FHE_EXECUTE_DISCRIMINATOR }),
  );
}

export function getFheExecuteInstructionDataDecoder(): Decoder<FheExecuteInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['accountCount', getU8Decoder()],
    ['dictionary', getArrayDecoder(fixDecoderSize(getBytesDecoder(), 32))],
    ['steps', getArrayDecoder(getFheExecuteStepDecoder())],
  ]);
}

export function getFheExecuteInstructionDataCodec(): Codec<FheExecuteInstructionDataArgs, FheExecuteInstructionData> {
  return combineCodec(getFheExecuteInstructionDataEncoder(), getFheExecuteInstructionDataDecoder());
}

export type FheExecuteAsyncInput<
  TAccountPayer extends string = string,
  TAccountComputeSubject extends string = string,
  TAccountEncryptedValueAccountAuthority extends string = string,
  TAccountHostConfig extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountHcuBlockMeter extends string = string,
  TAccountHcuTrustedAppRecord extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** Pays rent for any persistent output ACL records. */
  payer: TransactionSigner<TAccountPayer>;
  /** Compute subject that must be allowed on persistent encrypted inputs. */
  computeSubject: TransactionSigner<TAccountComputeSubject>;
  /**
   * Default authority signer: signs for every persistent output that does not name an authority
   * of its own. An output that sets `authority_index` points at a remaining account instead, and
   * that account must sign and must equal the authority the output declares.
   */
  encryptedValueAccountAuthority: TransactionSigner<TAccountEncryptedValueAccountAuthority>;
  /**
   * Singleton config PDA. Read-only: the cap is read from here, but the writable per-slot
   * counter is the separate `hcu_block_meter`, never this singleton — so the hot path takes no
   * write lock on the config.
   */
  hostConfig?: Address<TAccountHostConfig>;
  /** System program used for persistent output ACL creation. */
  systemProgram?: Address<TAccountSystemProgram>;
  /**
   * Per-`compute_subject` HCU block meter (written once in the execution `charge`). The HCU PDAs
   * (`hcu_block_meter`, `hcu_trusted_app_record`) key on `compute_subject` — the mandatory signed
   * caller identity already used for ACL admission — so no caller can rotate a fresh signer to
   * mint a fresh per-slot meter. Untrusted subjects in the metering band MUST supply this meter;
   * trusted subjects and the unrestricted default omit it. An `UncheckedAccount` because it may
   * be uninitialized (lazy-created) and is validated manually.
   */
  hcuBlockMeter?: Address<TAccountHcuBlockMeter>;
  /**
   * Trust witness (read-only), keyed on `compute_subject`. Present + program-owned +
   * `trusted == true` ⇒ bypass the cap; absent (`None`) ⇒ untrusted, fall through to the meter;
   * present-but-malformed ⇒ reject.
   */
  hcuTrustedAppRecord?: Address<TAccountHcuTrustedAppRecord>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  accountCount: FheExecuteInstructionDataArgs['accountCount'];
  dictionary: FheExecuteInstructionDataArgs['dictionary'];
  steps: FheExecuteInstructionDataArgs['steps'];
};

export async function getFheExecuteInstructionAsync<
  TAccountPayer extends string,
  TAccountComputeSubject extends string,
  TAccountEncryptedValueAccountAuthority extends string,
  TAccountHostConfig extends string,
  TAccountSystemProgram extends string,
  TAccountHcuBlockMeter extends string,
  TAccountHcuTrustedAppRecord extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: FheExecuteAsyncInput<
    TAccountPayer,
    TAccountComputeSubject,
    TAccountEncryptedValueAccountAuthority,
    TAccountHostConfig,
    TAccountSystemProgram,
    TAccountHcuBlockMeter,
    TAccountHcuTrustedAppRecord,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  FheExecuteInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountComputeSubject,
    TAccountEncryptedValueAccountAuthority,
    TAccountHostConfig,
    TAccountSystemProgram,
    TAccountHcuBlockMeter,
    TAccountHcuTrustedAppRecord,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    computeSubject: { value: input.computeSubject ?? null, isWritable: false },
    encryptedValueAccountAuthority: {
      value: input.encryptedValueAccountAuthority ?? null,
      isWritable: false,
    },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    hcuBlockMeter: { value: input.hcuBlockMeter ?? null, isWritable: true },
    hcuTrustedAppRecord: {
      value: input.hcuTrustedAppRecord ?? null,
      isWritable: false,
    },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hostConfig.value) {
    accounts.hostConfig.value = await findHostConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('computeSubject', accounts.computeSubject),
      getAccountMeta('encryptedValueAccountAuthority', accounts.encryptedValueAccountAuthority),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('systemProgram', accounts.systemProgram),
      getAccountMeta('hcuBlockMeter', accounts.hcuBlockMeter),
      getAccountMeta('hcuTrustedAppRecord', accounts.hcuTrustedAppRecord),
      getAccountMeta('eventAuthority', accounts.eventAuthority),
      getAccountMeta('program', accounts.program),
    ],
    data: getFheExecuteInstructionDataEncoder().encode(args as FheExecuteInstructionDataArgs),
    programAddress,
  } as FheExecuteInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountComputeSubject,
    TAccountEncryptedValueAccountAuthority,
    TAccountHostConfig,
    TAccountSystemProgram,
    TAccountHcuBlockMeter,
    TAccountHcuTrustedAppRecord,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type FheExecuteInput<
  TAccountPayer extends string = string,
  TAccountComputeSubject extends string = string,
  TAccountEncryptedValueAccountAuthority extends string = string,
  TAccountHostConfig extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountHcuBlockMeter extends string = string,
  TAccountHcuTrustedAppRecord extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** Pays rent for any persistent output ACL records. */
  payer: TransactionSigner<TAccountPayer>;
  /** Compute subject that must be allowed on persistent encrypted inputs. */
  computeSubject: TransactionSigner<TAccountComputeSubject>;
  /**
   * Default authority signer: signs for every persistent output that does not name an authority
   * of its own. An output that sets `authority_index` points at a remaining account instead, and
   * that account must sign and must equal the authority the output declares.
   */
  encryptedValueAccountAuthority: TransactionSigner<TAccountEncryptedValueAccountAuthority>;
  /**
   * Singleton config PDA. Read-only: the cap is read from here, but the writable per-slot
   * counter is the separate `hcu_block_meter`, never this singleton — so the hot path takes no
   * write lock on the config.
   */
  hostConfig: Address<TAccountHostConfig>;
  /** System program used for persistent output ACL creation. */
  systemProgram?: Address<TAccountSystemProgram>;
  /**
   * Per-`compute_subject` HCU block meter (written once in the execution `charge`). The HCU PDAs
   * (`hcu_block_meter`, `hcu_trusted_app_record`) key on `compute_subject` — the mandatory signed
   * caller identity already used for ACL admission — so no caller can rotate a fresh signer to
   * mint a fresh per-slot meter. Untrusted subjects in the metering band MUST supply this meter;
   * trusted subjects and the unrestricted default omit it. An `UncheckedAccount` because it may
   * be uninitialized (lazy-created) and is validated manually.
   */
  hcuBlockMeter?: Address<TAccountHcuBlockMeter>;
  /**
   * Trust witness (read-only), keyed on `compute_subject`. Present + program-owned +
   * `trusted == true` ⇒ bypass the cap; absent (`None`) ⇒ untrusted, fall through to the meter;
   * present-but-malformed ⇒ reject.
   */
  hcuTrustedAppRecord?: Address<TAccountHcuTrustedAppRecord>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  accountCount: FheExecuteInstructionDataArgs['accountCount'];
  dictionary: FheExecuteInstructionDataArgs['dictionary'];
  steps: FheExecuteInstructionDataArgs['steps'];
};

export function getFheExecuteInstruction<
  TAccountPayer extends string,
  TAccountComputeSubject extends string,
  TAccountEncryptedValueAccountAuthority extends string,
  TAccountHostConfig extends string,
  TAccountSystemProgram extends string,
  TAccountHcuBlockMeter extends string,
  TAccountHcuTrustedAppRecord extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: FheExecuteInput<
    TAccountPayer,
    TAccountComputeSubject,
    TAccountEncryptedValueAccountAuthority,
    TAccountHostConfig,
    TAccountSystemProgram,
    TAccountHcuBlockMeter,
    TAccountHcuTrustedAppRecord,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): FheExecuteInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountComputeSubject,
  TAccountEncryptedValueAccountAuthority,
  TAccountHostConfig,
  TAccountSystemProgram,
  TAccountHcuBlockMeter,
  TAccountHcuTrustedAppRecord,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    computeSubject: { value: input.computeSubject ?? null, isWritable: false },
    encryptedValueAccountAuthority: {
      value: input.encryptedValueAccountAuthority ?? null,
      isWritable: false,
    },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    hcuBlockMeter: { value: input.hcuBlockMeter ?? null, isWritable: true },
    hcuTrustedAppRecord: {
      value: input.hcuTrustedAppRecord ?? null,
      isWritable: false,
    },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('computeSubject', accounts.computeSubject),
      getAccountMeta('encryptedValueAccountAuthority', accounts.encryptedValueAccountAuthority),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('systemProgram', accounts.systemProgram),
      getAccountMeta('hcuBlockMeter', accounts.hcuBlockMeter),
      getAccountMeta('hcuTrustedAppRecord', accounts.hcuTrustedAppRecord),
      getAccountMeta('eventAuthority', accounts.eventAuthority),
      getAccountMeta('program', accounts.program),
    ],
    data: getFheExecuteInstructionDataEncoder().encode(args as FheExecuteInstructionDataArgs),
    programAddress,
  } as FheExecuteInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountComputeSubject,
    TAccountEncryptedValueAccountAuthority,
    TAccountHostConfig,
    TAccountSystemProgram,
    TAccountHcuBlockMeter,
    TAccountHcuTrustedAppRecord,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedFheExecuteInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays rent for any persistent output ACL records. */
    payer: TAccountMetas[0];
    /** Compute subject that must be allowed on persistent encrypted inputs. */
    computeSubject: TAccountMetas[1];
    /**
     * Default authority signer: signs for every persistent output that does not name an authority
     * of its own. An output that sets `authority_index` points at a remaining account instead, and
     * that account must sign and must equal the authority the output declares.
     */
    encryptedValueAccountAuthority: TAccountMetas[2];
    /**
     * Singleton config PDA. Read-only: the cap is read from here, but the writable per-slot
     * counter is the separate `hcu_block_meter`, never this singleton — so the hot path takes no
     * write lock on the config.
     */
    hostConfig: TAccountMetas[3];
    /** System program used for persistent output ACL creation. */
    systemProgram: TAccountMetas[4];
    /**
     * Per-`compute_subject` HCU block meter (written once in the execution `charge`). The HCU PDAs
     * (`hcu_block_meter`, `hcu_trusted_app_record`) key on `compute_subject` — the mandatory signed
     * caller identity already used for ACL admission — so no caller can rotate a fresh signer to
     * mint a fresh per-slot meter. Untrusted subjects in the metering band MUST supply this meter;
     * trusted subjects and the unrestricted default omit it. An `UncheckedAccount` because it may
     * be uninitialized (lazy-created) and is validated manually.
     */
    hcuBlockMeter?: TAccountMetas[5] | undefined;
    /**
     * Trust witness (read-only), keyed on `compute_subject`. Present + program-owned +
     * `trusted == true` ⇒ bypass the cap; absent (`None`) ⇒ untrusted, fall through to the meter;
     * present-but-malformed ⇒ reject.
     */
    hcuTrustedAppRecord?: TAccountMetas[6] | undefined;
    eventAuthority: TAccountMetas[7];
    program: TAccountMetas[8];
  };
  data: FheExecuteInstructionData;
};

export function parseFheExecuteInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedFheExecuteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 9,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === ZAMA_HOST_PROGRAM_ADDRESS ? undefined : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      computeSubject: getNextAccount(),
      encryptedValueAccountAuthority: getNextAccount(),
      hostConfig: getNextAccount(),
      systemProgram: getNextAccount(),
      hcuBlockMeter: getNextOptionalAccount(),
      hcuTrustedAppRecord: getNextOptionalAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getFheExecuteInstructionDataDecoder().decode(instruction.data),
  };
}
