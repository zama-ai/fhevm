/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findHostConfigPda } from '../pdas/index.js';
import { ZAMA_HOST_PROGRAM_ADDRESS } from '../programAddress.js';

export const MAKE_HANDLE_PUBLIC_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  66, 199, 252, 247, 244, 172, 42, 118,
]);

export function getMakeHandlePublicDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MAKE_HANDLE_PUBLIC_DISCRIMINATOR);
}

export type MakeHandlePublicInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountEncryptedValue extends string | AccountMeta<string> = string,
  TAccountHostConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountEncryptedValue extends string ? WritableAccount<TAccountEncryptedValue> : TAccountEncryptedValue,
      TAccountHostConfig extends string ? ReadonlyAccount<TAccountHostConfig> : TAccountHostConfig,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MakeHandlePublicInstructionData = {
  discriminator: ReadonlyUint8Array;
  handle: ReadonlyUint8Array;
};

export type MakeHandlePublicInstructionDataArgs = {
  handle: ReadonlyUint8Array;
};

export function getMakeHandlePublicInstructionDataEncoder(): FixedSizeEncoder<MakeHandlePublicInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['handle', fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: MAKE_HANDLE_PUBLIC_DISCRIMINATOR }),
  );
}

export function getMakeHandlePublicInstructionDataDecoder(): FixedSizeDecoder<MakeHandlePublicInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['handle', fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getMakeHandlePublicInstructionDataCodec(): FixedSizeCodec<
  MakeHandlePublicInstructionDataArgs,
  MakeHandlePublicInstructionData
> {
  return combineCodec(getMakeHandlePublicInstructionDataEncoder(), getMakeHandlePublicInstructionDataDecoder());
}

export type MakeHandlePublicAsyncInput<
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountEncryptedValue extends string = string,
  TAccountHostConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /**
   * Must equal `EncryptedValue.encrypted_value_account_authority`; decrypt subjects cannot
   * publish a handle.
   */
  authority: TransactionSigner<TAccountAuthority>;
  encryptedValue: Address<TAccountEncryptedValue>;
  hostConfig?: Address<TAccountHostConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  handle: MakeHandlePublicInstructionDataArgs['handle'];
};

export async function getMakeHandlePublicInstructionAsync<
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountEncryptedValue extends string,
  TAccountHostConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: MakeHandlePublicAsyncInput<
    TAccountPayer,
    TAccountAuthority,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  MakeHandlePublicInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    encryptedValue: { value: input.encryptedValue ?? null, isWritable: true },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hostConfig.value) {
    accounts.hostConfig.value = await findHostConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('encryptedValue', accounts.encryptedValue),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getMakeHandlePublicInstructionDataEncoder().encode(args as MakeHandlePublicInstructionDataArgs),
    programAddress,
  } as MakeHandlePublicInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountSystemProgram
  >);
}

export type MakeHandlePublicInput<
  TAccountPayer extends string = string,
  TAccountAuthority extends string = string,
  TAccountEncryptedValue extends string = string,
  TAccountHostConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  /**
   * Must equal `EncryptedValue.encrypted_value_account_authority`; decrypt subjects cannot
   * publish a handle.
   */
  authority: TransactionSigner<TAccountAuthority>;
  encryptedValue: Address<TAccountEncryptedValue>;
  hostConfig: Address<TAccountHostConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
  handle: MakeHandlePublicInstructionDataArgs['handle'];
};

export function getMakeHandlePublicInstruction<
  TAccountPayer extends string,
  TAccountAuthority extends string,
  TAccountEncryptedValue extends string,
  TAccountHostConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: MakeHandlePublicInput<
    TAccountPayer,
    TAccountAuthority,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): MakeHandlePublicInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountAuthority,
  TAccountEncryptedValue,
  TAccountHostConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    encryptedValue: { value: input.encryptedValue ?? null, isWritable: true },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('encryptedValue', accounts.encryptedValue),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getMakeHandlePublicInstructionDataEncoder().encode(args as MakeHandlePublicInstructionDataArgs),
    programAddress,
  } as MakeHandlePublicInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountAuthority,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountSystemProgram
  >);
}

export type ParsedMakeHandlePublicInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    /**
     * Must equal `EncryptedValue.encrypted_value_account_authority`; decrypt subjects cannot
     * publish a handle.
     */
    authority: TAccountMetas[1];
    encryptedValue: TAccountMetas[2];
    hostConfig: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: MakeHandlePublicInstructionData;
};

export function parseMakeHandlePublicInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedMakeHandlePublicInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 5,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      authority: getNextAccount(),
      encryptedValue: getNextAccount(),
      hostConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMakeHandlePublicInstructionDataDecoder().decode(instruction.data),
  };
}
