/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from '@solana/kit';

/**
 * The replaced state a persistent-output update declares: the stored handle
 * and the exact stored subject set, together (making a half-declared previous
 * state unrepresentable). Validated against the account, and carried in
 * instruction data so indexers reconstruct the appended MMR leaves without
 * reading the account.
 */
export type PreviousState = {
  /** The handle being replaced (`current_handle` at the time of the update). */
  handle: ReadonlyUint8Array;
  /** The exact stored subject set being replaced. */
  subjects: Array<Address>;
};

export type PreviousStateArgs = PreviousState;

export function getPreviousStateEncoder(): Encoder<PreviousStateArgs> {
  return getStructEncoder([
    ['handle', fixEncoderSize(getBytesEncoder(), 32)],
    ['subjects', getArrayEncoder(getAddressEncoder())],
  ]);
}

export function getPreviousStateDecoder(): Decoder<PreviousState> {
  return getStructDecoder([
    ['handle', fixDecoderSize(getBytesDecoder(), 32)],
    ['subjects', getArrayDecoder(getAddressDecoder())],
  ]);
}

export function getPreviousStateCodec(): Codec<PreviousStateArgs, PreviousState> {
  return combineCodec(getPreviousStateEncoder(), getPreviousStateDecoder());
}
