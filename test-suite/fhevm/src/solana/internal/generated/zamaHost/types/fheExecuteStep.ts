/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getDiscriminatedUnionDecoder,
  getDiscriminatedUnionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  type Codec,
  type Decoder,
  type Encoder,
  type GetDiscriminatedUnionVariant,
  type GetDiscriminatedUnionVariantContent,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getFheBinaryOpCodeDecoder,
  getFheBinaryOpCodeEncoder,
  getFheExecuteOperandDecoder,
  getFheExecuteOperandEncoder,
  getFheExecuteOutputDecoder,
  getFheExecuteOutputEncoder,
  getFheTernaryOpCodeDecoder,
  getFheTernaryOpCodeEncoder,
  getFheUnaryOpCodeDecoder,
  getFheUnaryOpCodeEncoder,
  type FheBinaryOpCode,
  type FheBinaryOpCodeArgs,
  type FheExecuteOperand,
  type FheExecuteOperandArgs,
  type FheExecuteOutput,
  type FheExecuteOutputArgs,
  type FheTernaryOpCode,
  type FheTernaryOpCodeArgs,
  type FheUnaryOpCode,
  type FheUnaryOpCodeArgs,
} from '.';

/** One step inside a composed fhe_execute. */
export type FheExecuteStep =
  | {
      __kind: 'Binary';
      /** Binary operator. */
      op: FheBinaryOpCode;
      /** Left-hand encrypted operand. */
      lhs: FheExecuteOperand;
      /** Right-hand encrypted operand or scalar bytes. */
      rhs: FheExecuteOperand;
      /** FHE type byte embedded in the output handle. */
      outputFheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutput;
    }
  | {
      __kind: 'Ternary';
      /** Ternary operator. */
      op: FheTernaryOpCode;
      /** Encrypted bool control operand. */
      control: FheExecuteOperand;
      /** Encrypted branch selected when control is true. */
      ifTrue: FheExecuteOperand;
      /** Encrypted branch selected when control is false. */
      ifFalse: FheExecuteOperand;
      /** FHE type byte embedded in the output handle. */
      outputFheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutput;
    }
  | {
      __kind: 'TrivialEncrypt';
      /** Plaintext bytes encoded using the host scalar convention. */
      plaintext: ReadonlyUint8Array;
      /** FHE type byte embedded in the output handle. */
      fheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutput;
    }
  | {
      __kind: 'Rand';
      /** FHE type byte embedded in the output handle. */
      fheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutput;
    }
  | {
      __kind: 'Unary';
      /** Unary operator. */
      op: FheUnaryOpCode;
      /** Encrypted operand. */
      operand: FheExecuteOperand;
      /** FHE type byte embedded in the output handle. */
      outputFheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutput;
    }
  | {
      __kind: 'RandBounded';
      /** Exclusive upper bound encoded as a 256-bit big-endian integer. */
      upperBound: ReadonlyUint8Array;
      /** FHE type byte embedded in the output handle. */
      fheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutput;
    }
  | {
      __kind: 'Sum';
      /** Encrypted operands. */
      operands: Array<FheExecuteOperand>;
      /** FHE type byte embedded in the output handle. */
      fheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutput;
    }
  | {
      __kind: 'IsIn';
      /** Encrypted value to test. */
      value: FheExecuteOperand;
      /** Encrypted set operands. */
      set: Array<FheExecuteOperand>;
      /** FHE type byte of the value and set elements. */
      fheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutput;
    }
  | {
      __kind: 'MulDiv';
      /** Left-hand encrypted factor. */
      factor1: FheExecuteOperand;
      /** Right-hand factor, encrypted or scalar bytes. */
      factor2: FheExecuteOperand;
      /** Divisor encoded as a 256-bit big-endian integer. */
      divisor: ReadonlyUint8Array;
      /** FHE type byte embedded in the output handle. */
      outputFheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutput;
    };

export type FheExecuteStepArgs =
  | {
      __kind: 'Binary';
      /** Binary operator. */
      op: FheBinaryOpCodeArgs;
      /** Left-hand encrypted operand. */
      lhs: FheExecuteOperandArgs;
      /** Right-hand encrypted operand or scalar bytes. */
      rhs: FheExecuteOperandArgs;
      /** FHE type byte embedded in the output handle. */
      outputFheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutputArgs;
    }
  | {
      __kind: 'Ternary';
      /** Ternary operator. */
      op: FheTernaryOpCodeArgs;
      /** Encrypted bool control operand. */
      control: FheExecuteOperandArgs;
      /** Encrypted branch selected when control is true. */
      ifTrue: FheExecuteOperandArgs;
      /** Encrypted branch selected when control is false. */
      ifFalse: FheExecuteOperandArgs;
      /** FHE type byte embedded in the output handle. */
      outputFheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutputArgs;
    }
  | {
      __kind: 'TrivialEncrypt';
      /** Plaintext bytes encoded using the host scalar convention. */
      plaintext: ReadonlyUint8Array;
      /** FHE type byte embedded in the output handle. */
      fheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutputArgs;
    }
  | {
      __kind: 'Rand';
      /** FHE type byte embedded in the output handle. */
      fheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutputArgs;
    }
  | {
      __kind: 'Unary';
      /** Unary operator. */
      op: FheUnaryOpCodeArgs;
      /** Encrypted operand. */
      operand: FheExecuteOperandArgs;
      /** FHE type byte embedded in the output handle. */
      outputFheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutputArgs;
    }
  | {
      __kind: 'RandBounded';
      /** Exclusive upper bound encoded as a 256-bit big-endian integer. */
      upperBound: ReadonlyUint8Array;
      /** FHE type byte embedded in the output handle. */
      fheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutputArgs;
    }
  | {
      __kind: 'Sum';
      /** Encrypted operands. */
      operands: Array<FheExecuteOperandArgs>;
      /** FHE type byte embedded in the output handle. */
      fheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutputArgs;
    }
  | {
      __kind: 'IsIn';
      /** Encrypted value to test. */
      value: FheExecuteOperandArgs;
      /** Encrypted set operands. */
      set: Array<FheExecuteOperandArgs>;
      /** FHE type byte of the value and set elements. */
      fheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutputArgs;
    }
  | {
      __kind: 'MulDiv';
      /** Left-hand encrypted factor. */
      factor1: FheExecuteOperandArgs;
      /** Right-hand factor, encrypted or scalar bytes. */
      factor2: FheExecuteOperandArgs;
      /** Divisor encoded as a 256-bit big-endian integer. */
      divisor: ReadonlyUint8Array;
      /** FHE type byte embedded in the output handle. */
      outputFheType: number;
      /** Whether this output remains instruction-local or is bound into persistent ACL state. */
      output: FheExecuteOutputArgs;
    };

export function getFheExecuteStepEncoder(): Encoder<FheExecuteStepArgs> {
  return getDiscriminatedUnionEncoder([
    [
      'Binary',
      getStructEncoder([
        ['op', getFheBinaryOpCodeEncoder()],
        ['lhs', getFheExecuteOperandEncoder()],
        ['rhs', getFheExecuteOperandEncoder()],
        ['outputFheType', getU8Encoder()],
        ['output', getFheExecuteOutputEncoder()],
      ]),
    ],
    [
      'Ternary',
      getStructEncoder([
        ['op', getFheTernaryOpCodeEncoder()],
        ['control', getFheExecuteOperandEncoder()],
        ['ifTrue', getFheExecuteOperandEncoder()],
        ['ifFalse', getFheExecuteOperandEncoder()],
        ['outputFheType', getU8Encoder()],
        ['output', getFheExecuteOutputEncoder()],
      ]),
    ],
    [
      'TrivialEncrypt',
      getStructEncoder([
        ['plaintext', fixEncoderSize(getBytesEncoder(), 32)],
        ['fheType', getU8Encoder()],
        ['output', getFheExecuteOutputEncoder()],
      ]),
    ],
    [
      'Rand',
      getStructEncoder([
        ['fheType', getU8Encoder()],
        ['output', getFheExecuteOutputEncoder()],
      ]),
    ],
    [
      'Unary',
      getStructEncoder([
        ['op', getFheUnaryOpCodeEncoder()],
        ['operand', getFheExecuteOperandEncoder()],
        ['outputFheType', getU8Encoder()],
        ['output', getFheExecuteOutputEncoder()],
      ]),
    ],
    [
      'RandBounded',
      getStructEncoder([
        ['upperBound', fixEncoderSize(getBytesEncoder(), 32)],
        ['fheType', getU8Encoder()],
        ['output', getFheExecuteOutputEncoder()],
      ]),
    ],
    [
      'Sum',
      getStructEncoder([
        ['operands', getArrayEncoder(getFheExecuteOperandEncoder())],
        ['fheType', getU8Encoder()],
        ['output', getFheExecuteOutputEncoder()],
      ]),
    ],
    [
      'IsIn',
      getStructEncoder([
        ['value', getFheExecuteOperandEncoder()],
        ['set', getArrayEncoder(getFheExecuteOperandEncoder())],
        ['fheType', getU8Encoder()],
        ['output', getFheExecuteOutputEncoder()],
      ]),
    ],
    [
      'MulDiv',
      getStructEncoder([
        ['factor1', getFheExecuteOperandEncoder()],
        ['factor2', getFheExecuteOperandEncoder()],
        ['divisor', fixEncoderSize(getBytesEncoder(), 32)],
        ['outputFheType', getU8Encoder()],
        ['output', getFheExecuteOutputEncoder()],
      ]),
    ],
  ]);
}

export function getFheExecuteStepDecoder(): Decoder<FheExecuteStep> {
  return getDiscriminatedUnionDecoder([
    [
      'Binary',
      getStructDecoder([
        ['op', getFheBinaryOpCodeDecoder()],
        ['lhs', getFheExecuteOperandDecoder()],
        ['rhs', getFheExecuteOperandDecoder()],
        ['outputFheType', getU8Decoder()],
        ['output', getFheExecuteOutputDecoder()],
      ]),
    ],
    [
      'Ternary',
      getStructDecoder([
        ['op', getFheTernaryOpCodeDecoder()],
        ['control', getFheExecuteOperandDecoder()],
        ['ifTrue', getFheExecuteOperandDecoder()],
        ['ifFalse', getFheExecuteOperandDecoder()],
        ['outputFheType', getU8Decoder()],
        ['output', getFheExecuteOutputDecoder()],
      ]),
    ],
    [
      'TrivialEncrypt',
      getStructDecoder([
        ['plaintext', fixDecoderSize(getBytesDecoder(), 32)],
        ['fheType', getU8Decoder()],
        ['output', getFheExecuteOutputDecoder()],
      ]),
    ],
    [
      'Rand',
      getStructDecoder([
        ['fheType', getU8Decoder()],
        ['output', getFheExecuteOutputDecoder()],
      ]),
    ],
    [
      'Unary',
      getStructDecoder([
        ['op', getFheUnaryOpCodeDecoder()],
        ['operand', getFheExecuteOperandDecoder()],
        ['outputFheType', getU8Decoder()],
        ['output', getFheExecuteOutputDecoder()],
      ]),
    ],
    [
      'RandBounded',
      getStructDecoder([
        ['upperBound', fixDecoderSize(getBytesDecoder(), 32)],
        ['fheType', getU8Decoder()],
        ['output', getFheExecuteOutputDecoder()],
      ]),
    ],
    [
      'Sum',
      getStructDecoder([
        ['operands', getArrayDecoder(getFheExecuteOperandDecoder())],
        ['fheType', getU8Decoder()],
        ['output', getFheExecuteOutputDecoder()],
      ]),
    ],
    [
      'IsIn',
      getStructDecoder([
        ['value', getFheExecuteOperandDecoder()],
        ['set', getArrayDecoder(getFheExecuteOperandDecoder())],
        ['fheType', getU8Decoder()],
        ['output', getFheExecuteOutputDecoder()],
      ]),
    ],
    [
      'MulDiv',
      getStructDecoder([
        ['factor1', getFheExecuteOperandDecoder()],
        ['factor2', getFheExecuteOperandDecoder()],
        ['divisor', fixDecoderSize(getBytesDecoder(), 32)],
        ['outputFheType', getU8Decoder()],
        ['output', getFheExecuteOutputDecoder()],
      ]),
    ],
  ]);
}

export function getFheExecuteStepCodec(): Codec<FheExecuteStepArgs, FheExecuteStep> {
  return combineCodec(getFheExecuteStepEncoder(), getFheExecuteStepDecoder());
}

// Data Enum Helpers.
export function fheExecuteStep(
  kind: 'Binary',
  data: GetDiscriminatedUnionVariantContent<FheExecuteStepArgs, '__kind', 'Binary'>,
): GetDiscriminatedUnionVariant<FheExecuteStepArgs, '__kind', 'Binary'>;
export function fheExecuteStep(
  kind: 'Ternary',
  data: GetDiscriminatedUnionVariantContent<FheExecuteStepArgs, '__kind', 'Ternary'>,
): GetDiscriminatedUnionVariant<FheExecuteStepArgs, '__kind', 'Ternary'>;
export function fheExecuteStep(
  kind: 'TrivialEncrypt',
  data: GetDiscriminatedUnionVariantContent<FheExecuteStepArgs, '__kind', 'TrivialEncrypt'>,
): GetDiscriminatedUnionVariant<FheExecuteStepArgs, '__kind', 'TrivialEncrypt'>;
export function fheExecuteStep(
  kind: 'Rand',
  data: GetDiscriminatedUnionVariantContent<FheExecuteStepArgs, '__kind', 'Rand'>,
): GetDiscriminatedUnionVariant<FheExecuteStepArgs, '__kind', 'Rand'>;
export function fheExecuteStep(
  kind: 'Unary',
  data: GetDiscriminatedUnionVariantContent<FheExecuteStepArgs, '__kind', 'Unary'>,
): GetDiscriminatedUnionVariant<FheExecuteStepArgs, '__kind', 'Unary'>;
export function fheExecuteStep(
  kind: 'RandBounded',
  data: GetDiscriminatedUnionVariantContent<FheExecuteStepArgs, '__kind', 'RandBounded'>,
): GetDiscriminatedUnionVariant<FheExecuteStepArgs, '__kind', 'RandBounded'>;
export function fheExecuteStep(
  kind: 'Sum',
  data: GetDiscriminatedUnionVariantContent<FheExecuteStepArgs, '__kind', 'Sum'>,
): GetDiscriminatedUnionVariant<FheExecuteStepArgs, '__kind', 'Sum'>;
export function fheExecuteStep(
  kind: 'IsIn',
  data: GetDiscriminatedUnionVariantContent<FheExecuteStepArgs, '__kind', 'IsIn'>,
): GetDiscriminatedUnionVariant<FheExecuteStepArgs, '__kind', 'IsIn'>;
export function fheExecuteStep(
  kind: 'MulDiv',
  data: GetDiscriminatedUnionVariantContent<FheExecuteStepArgs, '__kind', 'MulDiv'>,
): GetDiscriminatedUnionVariant<FheExecuteStepArgs, '__kind', 'MulDiv'>;
export function fheExecuteStep<K extends FheExecuteStepArgs['__kind'], Data>(kind: K, data?: Data) {
  return Array.isArray(data) ? { __kind: kind, fields: data } : { __kind: kind, ...(data ?? {}) };
}

export function isFheExecuteStep<K extends FheExecuteStep['__kind']>(
  kind: K,
  value: FheExecuteStep,
): value is FheExecuteStep & { __kind: K } {
  return value.__kind === kind;
}
