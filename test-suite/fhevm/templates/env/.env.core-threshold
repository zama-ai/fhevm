# Threshold-mode KMS cluster component (`core-threshold`).
# The generated cluster services take no env_file: per-party config and minio/AWS
# credentials are injected as KMS_CORE__* / AWS_* container environment by
# buildKmsThresholdOverride (see src/generate/kms-core.ts). This file exists only
# because the harness's per-component env loader expects a template per component;
# nothing reads RUST_LOG from here.
RUST_LOG=info
