apiVersion: fhevm.zama.ai/scenario/v1
kind: Scenario
metadata:
  id: fixture.passing
  name: Fixture passing
  owner: qa
  description: Engine test fixture (passing).
  tags: [fixture, fast]
spec:
  runtime: cucumber
  entrypoint: ./fixture.feature
  supportCode: [./steps.ts]
  timeoutSeconds: 30
