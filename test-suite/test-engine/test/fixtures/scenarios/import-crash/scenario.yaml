apiVersion: fhevm.zama.ai/scenario/v1
kind: Scenario
metadata:
  id: fixture.import-crash
  name: Fixture import-crash
  owner: qa
  description: Engine test fixture (import-crash).
  tags: [fixture, fast]
spec:
  runtime: cucumber
  entrypoint: ./fixture.feature
  supportCode: [./steps.ts]
  timeoutSeconds: 30
