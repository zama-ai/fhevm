apiVersion: fhevm.zama.ai/scenario/v1
kind: Scenario
metadata:
  id: fixture.parse-error
  name: Fixture parse-error
  owner: qa
  description: Engine test fixture (parse-error).
  tags: [fixture, fast]
spec:
  runtime: cucumber
  entrypoint: ./fixture.feature
  supportCode: [./steps.ts]
  timeoutSeconds: 30
