/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findBatchAuthorityPda } from '../pdas/index.js';
import { CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS } from '../programAddress.js';

export const RECLAIM_BATCH_AUTHORITY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  164, 167, 253, 18, 169, 242, 223, 206,
]);

export function getReclaimBatchAuthorityDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(RECLAIM_BATCH_AUTHORITY_DISCRIMINATOR);
}

export type ReclaimBatchAuthorityInstruction<
  TProgram extends string = typeof CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountBatcher extends string | AccountMeta<string> = string,
  TAccountBatch extends string | AccountMeta<string> = string,
  TAccountBatchAuthority extends string | AccountMeta<string> = string,
  TAccountJoinConfidentialMint extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountBatcher extends string ? ReadonlyAccount<TAccountBatcher> : TAccountBatcher,
      TAccountBatch extends string ? ReadonlyAccount<TAccountBatch> : TAccountBatch,
      TAccountBatchAuthority extends string ? WritableAccount<TAccountBatchAuthority> : TAccountBatchAuthority,
      TAccountJoinConfidentialMint extends string
        ? ReadonlyAccount<TAccountJoinConfidentialMint>
        : TAccountJoinConfidentialMint,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ReclaimBatchAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ReclaimBatchAuthorityInstructionDataArgs = {};

export function getReclaimBatchAuthorityInstructionDataEncoder(): FixedSizeEncoder<ReclaimBatchAuthorityInstructionDataArgs> {
  return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({
    ...value,
    discriminator: RECLAIM_BATCH_AUTHORITY_DISCRIMINATOR,
  }));
}

export function getReclaimBatchAuthorityInstructionDataDecoder(): FixedSizeDecoder<ReclaimBatchAuthorityInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getReclaimBatchAuthorityInstructionDataCodec(): FixedSizeCodec<
  ReclaimBatchAuthorityInstructionDataArgs,
  ReclaimBatchAuthorityInstructionData
> {
  return combineCodec(
    getReclaimBatchAuthorityInstructionDataEncoder(),
    getReclaimBatchAuthorityInstructionDataDecoder(),
  );
}

export type ReclaimBatchAuthorityAsyncInput<
  TAccountAuthority extends string = string,
  TAccountBatcher extends string = string,
  TAccountBatch extends string = string,
  TAccountBatchAuthority extends string = string,
  TAccountJoinConfidentialMint extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Join-mint wrapper authority; receives the lamports. */
  authority: TransactionSigner<TAccountAuthority>;
  /** Batcher config. */
  batcher: Address<TAccountBatcher>;
  /** The finished batch (settled, canceled or refunding). */
  batch: Address<TAccountBatch>;
  batchAuthority?: Address<TAccountBatchAuthority>;
  /** Confidential mint users join batches with; its authority is the reclaim authority. */
  joinConfidentialMint: Address<TAccountJoinConfidentialMint>;
  /** System program that moves the lamports. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getReclaimBatchAuthorityInstructionAsync<
  TAccountAuthority extends string,
  TAccountBatcher extends string,
  TAccountBatch extends string,
  TAccountBatchAuthority extends string,
  TAccountJoinConfidentialMint extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS,
>(
  input: ReclaimBatchAuthorityAsyncInput<
    TAccountAuthority,
    TAccountBatcher,
    TAccountBatch,
    TAccountBatchAuthority,
    TAccountJoinConfidentialMint,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ReclaimBatchAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBatcher,
    TAccountBatch,
    TAccountBatchAuthority,
    TAccountJoinConfidentialMint,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    batcher: { value: input.batcher ?? null, isWritable: false },
    batch: { value: input.batch ?? null, isWritable: false },
    batchAuthority: { value: input.batchAuthority ?? null, isWritable: true },
    joinConfidentialMint: {
      value: input.joinConfidentialMint ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Resolve default values.
  if (!accounts.batchAuthority.value) {
    accounts.batchAuthority.value = await findBatchAuthorityPda({
      batch: getAddressFromResolvedInstructionAccount('batch', accounts.batch.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('batcher', accounts.batcher),
      getAccountMeta('batch', accounts.batch),
      getAccountMeta('batchAuthority', accounts.batchAuthority),
      getAccountMeta('joinConfidentialMint', accounts.joinConfidentialMint),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getReclaimBatchAuthorityInstructionDataEncoder().encode({}),
    programAddress,
  } as ReclaimBatchAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBatcher,
    TAccountBatch,
    TAccountBatchAuthority,
    TAccountJoinConfidentialMint,
    TAccountSystemProgram
  >);
}

export type ReclaimBatchAuthorityInput<
  TAccountAuthority extends string = string,
  TAccountBatcher extends string = string,
  TAccountBatch extends string = string,
  TAccountBatchAuthority extends string = string,
  TAccountJoinConfidentialMint extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Join-mint wrapper authority; receives the lamports. */
  authority: TransactionSigner<TAccountAuthority>;
  /** Batcher config. */
  batcher: Address<TAccountBatcher>;
  /** The finished batch (settled, canceled or refunding). */
  batch: Address<TAccountBatch>;
  batchAuthority: Address<TAccountBatchAuthority>;
  /** Confidential mint users join batches with; its authority is the reclaim authority. */
  joinConfidentialMint: Address<TAccountJoinConfidentialMint>;
  /** System program that moves the lamports. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getReclaimBatchAuthorityInstruction<
  TAccountAuthority extends string,
  TAccountBatcher extends string,
  TAccountBatch extends string,
  TAccountBatchAuthority extends string,
  TAccountJoinConfidentialMint extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS,
>(
  input: ReclaimBatchAuthorityInput<
    TAccountAuthority,
    TAccountBatcher,
    TAccountBatch,
    TAccountBatchAuthority,
    TAccountJoinConfidentialMint,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ReclaimBatchAuthorityInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountBatcher,
  TAccountBatch,
  TAccountBatchAuthority,
  TAccountJoinConfidentialMint,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    batcher: { value: input.batcher ?? null, isWritable: false },
    batch: { value: input.batch ?? null, isWritable: false },
    batchAuthority: { value: input.batchAuthority ?? null, isWritable: true },
    joinConfidentialMint: {
      value: input.joinConfidentialMint ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('authority', accounts.authority),
      getAccountMeta('batcher', accounts.batcher),
      getAccountMeta('batch', accounts.batch),
      getAccountMeta('batchAuthority', accounts.batchAuthority),
      getAccountMeta('joinConfidentialMint', accounts.joinConfidentialMint),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getReclaimBatchAuthorityInstructionDataEncoder().encode({}),
    programAddress,
  } as ReclaimBatchAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountBatcher,
    TAccountBatch,
    TAccountBatchAuthority,
    TAccountJoinConfidentialMint,
    TAccountSystemProgram
  >);
}

export type ParsedReclaimBatchAuthorityInstruction<
  TProgram extends string = typeof CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Join-mint wrapper authority; receives the lamports. */
    authority: TAccountMetas[0];
    /** Batcher config. */
    batcher: TAccountMetas[1];
    /** The finished batch (settled, canceled or refunding). */
    batch: TAccountMetas[2];
    batchAuthority: TAccountMetas[3];
    /** Confidential mint users join batches with; its authority is the reclaim authority. */
    joinConfidentialMint: TAccountMetas[4];
    /** System program that moves the lamports. */
    systemProgram: TAccountMetas[5];
  };
  data: ReclaimBatchAuthorityInstructionData;
};

export function parseReclaimBatchAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedReclaimBatchAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 6,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      batcher: getNextAccount(),
      batch: getNextAccount(),
      batchAuthority: getNextAccount(),
      joinConfidentialMint: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getReclaimBatchAuthorityInstructionDataDecoder().decode(instruction.data),
  };
}
