/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findJoinRecordPda } from '../pdas/index.js';
import { CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS } from '../programAddress.js';

export const CLOSE_JOIN_RECORD_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  214, 253, 223, 106, 110, 247, 230, 167,
]);

export function getCloseJoinRecordDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_JOIN_RECORD_DISCRIMINATOR);
}

export type CloseJoinRecordInstruction<
  TProgram extends string = typeof CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountBatch extends string | AccountMeta<string> = string,
  TAccountJoinRecord extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountBatch extends string ? ReadonlyAccount<TAccountBatch> : TAccountBatch,
      TAccountJoinRecord extends string ? WritableAccount<TAccountJoinRecord> : TAccountJoinRecord,
      ...TRemainingAccounts,
    ]
  >;

export type CloseJoinRecordInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CloseJoinRecordInstructionDataArgs = {};

export function getCloseJoinRecordInstructionDataEncoder(): FixedSizeEncoder<CloseJoinRecordInstructionDataArgs> {
  return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({
    ...value,
    discriminator: CLOSE_JOIN_RECORD_DISCRIMINATOR,
  }));
}

export function getCloseJoinRecordInstructionDataDecoder(): FixedSizeDecoder<CloseJoinRecordInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getCloseJoinRecordInstructionDataCodec(): FixedSizeCodec<
  CloseJoinRecordInstructionDataArgs,
  CloseJoinRecordInstructionData
> {
  return combineCodec(getCloseJoinRecordInstructionDataEncoder(), getCloseJoinRecordInstructionDataDecoder());
}

export type CloseJoinRecordAsyncInput<
  TAccountUser extends string = string,
  TAccountBatch extends string = string,
  TAccountJoinRecord extends string = string,
> = {
  /** The user who joined; receives the record's rent. */
  user: TransactionSigner<TAccountUser>;
  /** The batch the record belongs to. */
  batch: Address<TAccountBatch>;
  /** The user's join record for this batch; closed to `user`. */
  joinRecord?: Address<TAccountJoinRecord>;
};

export async function getCloseJoinRecordInstructionAsync<
  TAccountUser extends string,
  TAccountBatch extends string,
  TAccountJoinRecord extends string,
  TProgramAddress extends Address = typeof CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS,
>(
  input: CloseJoinRecordAsyncInput<TAccountUser, TAccountBatch, TAccountJoinRecord>,
  config?: { programAddress?: TProgramAddress },
): Promise<CloseJoinRecordInstruction<TProgramAddress, TAccountUser, TAccountBatch, TAccountJoinRecord>> {
  // Program address.
  const programAddress = config?.programAddress ?? CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    batch: { value: input.batch ?? null, isWritable: false },
    joinRecord: { value: input.joinRecord ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Resolve default values.
  if (!accounts.joinRecord.value) {
    accounts.joinRecord.value = await findJoinRecordPda({
      batch: getAddressFromResolvedInstructionAccount('batch', accounts.batch.value),
      user: getAddressFromResolvedInstructionAccount('user', accounts.user.value),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('user', accounts.user),
      getAccountMeta('batch', accounts.batch),
      getAccountMeta('joinRecord', accounts.joinRecord),
    ],
    data: getCloseJoinRecordInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseJoinRecordInstruction<TProgramAddress, TAccountUser, TAccountBatch, TAccountJoinRecord>);
}

export type CloseJoinRecordInput<
  TAccountUser extends string = string,
  TAccountBatch extends string = string,
  TAccountJoinRecord extends string = string,
> = {
  /** The user who joined; receives the record's rent. */
  user: TransactionSigner<TAccountUser>;
  /** The batch the record belongs to. */
  batch: Address<TAccountBatch>;
  /** The user's join record for this batch; closed to `user`. */
  joinRecord: Address<TAccountJoinRecord>;
};

export function getCloseJoinRecordInstruction<
  TAccountUser extends string,
  TAccountBatch extends string,
  TAccountJoinRecord extends string,
  TProgramAddress extends Address = typeof CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS,
>(
  input: CloseJoinRecordInput<TAccountUser, TAccountBatch, TAccountJoinRecord>,
  config?: { programAddress?: TProgramAddress },
): CloseJoinRecordInstruction<TProgramAddress, TAccountUser, TAccountBatch, TAccountJoinRecord> {
  // Program address.
  const programAddress = config?.programAddress ?? CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    batch: { value: input.batch ?? null, isWritable: false },
    joinRecord: { value: input.joinRecord ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('user', accounts.user),
      getAccountMeta('batch', accounts.batch),
      getAccountMeta('joinRecord', accounts.joinRecord),
    ],
    data: getCloseJoinRecordInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseJoinRecordInstruction<TProgramAddress, TAccountUser, TAccountBatch, TAccountJoinRecord>);
}

export type ParsedCloseJoinRecordInstruction<
  TProgram extends string = typeof CONFIDENTIAL_BATCHER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The user who joined; receives the record's rent. */
    user: TAccountMetas[0];
    /** The batch the record belongs to. */
    batch: TAccountMetas[1];
    /** The user's join record for this batch; closed to `user`. */
    joinRecord: TAccountMetas[2];
  };
  data: CloseJoinRecordInstructionData;
};

export function parseCloseJoinRecordInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseJoinRecordInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      batch: getNextAccount(),
      joinRecord: getNextAccount(),
    },
    data: getCloseJoinRecordInstructionDataDecoder().decode(instruction.data),
  };
}
