/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getBatchDirectionDecoder,
  getBatchDirectionEncoder,
  type BatchDirection,
  type BatchDirectionArgs,
} from '../types/index.js';

export const BATCHER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([101, 170, 246, 196, 102, 232, 50, 147]);

export function getBatcherDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BATCHER_DISCRIMINATOR);
}

export type Batcher = {
  discriminator: ReadonlyUint8Array;
  /** Direction of this batcher instance. */
  direction: BatchDirection;
  /**
   * Confidential mint users join batches with. Wraps the vault's underlying
   * mint for deposit batchers, the vault's share mint for redeem batchers.
   */
  joinConfidentialMint: Address;
  /**
   * Confidential mint claims pay out in. Wraps the vault's share mint for
   * deposit batchers, the vault's underlying mint for redeem batchers.
   */
  payoutConfidentialMint: Address;
  /** Public `demo_vault::Vault` the batcher fronts. */
  vault: Address;
  /** Minimum slots a batch must stay open before dispatch. */
  minBatchAgeSlots: bigint;
  /** Index the next `open_batch` creates. */
  nextBatchIndex: bigint;
};

export type BatcherArgs = {
  /** Direction of this batcher instance. */
  direction: BatchDirectionArgs;
  /**
   * Confidential mint users join batches with. Wraps the vault's underlying
   * mint for deposit batchers, the vault's share mint for redeem batchers.
   */
  joinConfidentialMint: Address;
  /**
   * Confidential mint claims pay out in. Wraps the vault's share mint for
   * deposit batchers, the vault's underlying mint for redeem batchers.
   */
  payoutConfidentialMint: Address;
  /** Public `demo_vault::Vault` the batcher fronts. */
  vault: Address;
  /** Minimum slots a batch must stay open before dispatch. */
  minBatchAgeSlots: number | bigint;
  /** Index the next `open_batch` creates. */
  nextBatchIndex: number | bigint;
};

/** Gets the encoder for {@link BatcherArgs} account data. */
export function getBatcherEncoder(): FixedSizeEncoder<BatcherArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['direction', getBatchDirectionEncoder()],
      ['joinConfidentialMint', getAddressEncoder()],
      ['payoutConfidentialMint', getAddressEncoder()],
      ['vault', getAddressEncoder()],
      ['minBatchAgeSlots', getU64Encoder()],
      ['nextBatchIndex', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BATCHER_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Batcher} account data. */
export function getBatcherDecoder(): FixedSizeDecoder<Batcher> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['direction', getBatchDirectionDecoder()],
    ['joinConfidentialMint', getAddressDecoder()],
    ['payoutConfidentialMint', getAddressDecoder()],
    ['vault', getAddressDecoder()],
    ['minBatchAgeSlots', getU64Decoder()],
    ['nextBatchIndex', getU64Decoder()],
  ]);
}

/** Gets the codec for {@link Batcher} account data. */
export function getBatcherCodec(): FixedSizeCodec<BatcherArgs, Batcher> {
  return combineCodec(getBatcherEncoder(), getBatcherDecoder());
}

export function decodeBatcher<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Batcher, TAddress>;
export function decodeBatcher<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Batcher, TAddress>;
export function decodeBatcher<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Batcher, TAddress> | MaybeAccount<Batcher, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getBatcherDecoder());
}

export async function fetchBatcher<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Batcher, TAddress>> {
  const maybeAccount = await fetchMaybeBatcher(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBatcher<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Batcher, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBatcher(maybeAccount);
}

export async function fetchAllBatcher(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Batcher>[]> {
  const maybeAccounts = await fetchAllMaybeBatcher(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBatcher(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Batcher>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeBatcher(maybeAccount));
}

export function getBatcherSize(): number {
  return 121;
}
