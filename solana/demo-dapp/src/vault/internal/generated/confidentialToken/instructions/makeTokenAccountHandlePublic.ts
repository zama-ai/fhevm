/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findTokenAccountPda } from '../pdas/index.js';
import { CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS } from '../programAddress.js';
import {
  getDisclosedValueKindDecoder,
  getDisclosedValueKindEncoder,
  type DisclosedValueKind,
  type DisclosedValueKindArgs,
} from '../types/index.js';

export const MAKE_TOKEN_ACCOUNT_HANDLE_PUBLIC_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  59, 214, 155, 57, 204, 66, 251, 94,
]);

export function getMakeTokenAccountHandlePublicDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MAKE_TOKEN_ACCOUNT_HANDLE_PUBLIC_DISCRIMINATOR);
}

export type MakeTokenAccountHandlePublicInstruction<
  TProgram extends string = typeof CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountTokenAccount extends string | AccountMeta<string> = string,
  TAccountEncryptedValue extends string | AccountMeta<string> = string,
  TAccountHostConfig extends string | AccountMeta<string> = string,
  TAccountZamaProgram extends string | AccountMeta<string> = '6AtbvED1rfX68aCT1tYgU1aeu4kFksPDxZG9gtB1Fgtu',
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountMint extends string ? ReadonlyAccount<TAccountMint> : TAccountMint,
      TAccountTokenAccount extends string ? ReadonlyAccount<TAccountTokenAccount> : TAccountTokenAccount,
      TAccountEncryptedValue extends string ? WritableAccount<TAccountEncryptedValue> : TAccountEncryptedValue,
      TAccountHostConfig extends string ? ReadonlyAccount<TAccountHostConfig> : TAccountHostConfig,
      TAccountZamaProgram extends string ? ReadonlyAccount<TAccountZamaProgram> : TAccountZamaProgram,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MakeTokenAccountHandlePublicInstructionData = {
  discriminator: ReadonlyUint8Array;
  kind: DisclosedValueKind;
  handle: ReadonlyUint8Array;
};

export type MakeTokenAccountHandlePublicInstructionDataArgs = {
  kind: DisclosedValueKindArgs;
  handle: ReadonlyUint8Array;
};

export function getMakeTokenAccountHandlePublicInstructionDataEncoder(): FixedSizeEncoder<MakeTokenAccountHandlePublicInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['kind', getDisclosedValueKindEncoder()],
      ['handle', fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({
      ...value,
      discriminator: MAKE_TOKEN_ACCOUNT_HANDLE_PUBLIC_DISCRIMINATOR,
    }),
  );
}

export function getMakeTokenAccountHandlePublicInstructionDataDecoder(): FixedSizeDecoder<MakeTokenAccountHandlePublicInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['kind', getDisclosedValueKindDecoder()],
    ['handle', fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getMakeTokenAccountHandlePublicInstructionDataCodec(): FixedSizeCodec<
  MakeTokenAccountHandlePublicInstructionDataArgs,
  MakeTokenAccountHandlePublicInstructionData
> {
  return combineCodec(
    getMakeTokenAccountHandlePublicInstructionDataEncoder(),
    getMakeTokenAccountHandlePublicInstructionDataDecoder(),
  );
}

export type MakeTokenAccountHandlePublicAsyncInput<
  TAccountPayer extends string = string,
  TAccountOwner extends string = string,
  TAccountMint extends string = string,
  TAccountTokenAccount extends string = string,
  TAccountEncryptedValue extends string = string,
  TAccountHostConfig extends string = string,
  TAccountZamaProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  owner: TransactionSigner<TAccountOwner>;
  mint: Address<TAccountMint>;
  tokenAccount?: Address<TAccountTokenAccount>;
  encryptedValue: Address<TAccountEncryptedValue>;
  hostConfig: Address<TAccountHostConfig>;
  zamaProgram?: Address<TAccountZamaProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  kind: MakeTokenAccountHandlePublicInstructionDataArgs['kind'];
  handle: MakeTokenAccountHandlePublicInstructionDataArgs['handle'];
};

export async function getMakeTokenAccountHandlePublicInstructionAsync<
  TAccountPayer extends string,
  TAccountOwner extends string,
  TAccountMint extends string,
  TAccountTokenAccount extends string,
  TAccountEncryptedValue extends string,
  TAccountHostConfig extends string,
  TAccountZamaProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS,
>(
  input: MakeTokenAccountHandlePublicAsyncInput<
    TAccountPayer,
    TAccountOwner,
    TAccountMint,
    TAccountTokenAccount,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountZamaProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  MakeTokenAccountHandlePublicInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountMint,
    TAccountTokenAccount,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountZamaProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    tokenAccount: { value: input.tokenAccount ?? null, isWritable: false },
    encryptedValue: { value: input.encryptedValue ?? null, isWritable: true },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    zamaProgram: { value: input.zamaProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenAccount.value) {
    accounts.tokenAccount.value = await findTokenAccountPda({
      mint: getAddressFromResolvedInstructionAccount('mint', accounts.mint.value),
      owner: getAddressFromResolvedInstructionAccount('owner', accounts.owner.value),
    });
  }
  if (!accounts.zamaProgram.value) {
    accounts.zamaProgram.value =
      '6AtbvED1rfX68aCT1tYgU1aeu4kFksPDxZG9gtB1Fgtu' as Address<'6AtbvED1rfX68aCT1tYgU1aeu4kFksPDxZG9gtB1Fgtu'>;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('owner', accounts.owner),
      getAccountMeta('mint', accounts.mint),
      getAccountMeta('tokenAccount', accounts.tokenAccount),
      getAccountMeta('encryptedValue', accounts.encryptedValue),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('zamaProgram', accounts.zamaProgram),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getMakeTokenAccountHandlePublicInstructionDataEncoder().encode(
      args as MakeTokenAccountHandlePublicInstructionDataArgs,
    ),
    programAddress,
  } as MakeTokenAccountHandlePublicInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountMint,
    TAccountTokenAccount,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountZamaProgram,
    TAccountSystemProgram
  >);
}

export type MakeTokenAccountHandlePublicInput<
  TAccountPayer extends string = string,
  TAccountOwner extends string = string,
  TAccountMint extends string = string,
  TAccountTokenAccount extends string = string,
  TAccountEncryptedValue extends string = string,
  TAccountHostConfig extends string = string,
  TAccountZamaProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  owner: TransactionSigner<TAccountOwner>;
  mint: Address<TAccountMint>;
  tokenAccount: Address<TAccountTokenAccount>;
  encryptedValue: Address<TAccountEncryptedValue>;
  hostConfig: Address<TAccountHostConfig>;
  zamaProgram?: Address<TAccountZamaProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  kind: MakeTokenAccountHandlePublicInstructionDataArgs['kind'];
  handle: MakeTokenAccountHandlePublicInstructionDataArgs['handle'];
};

export function getMakeTokenAccountHandlePublicInstruction<
  TAccountPayer extends string,
  TAccountOwner extends string,
  TAccountMint extends string,
  TAccountTokenAccount extends string,
  TAccountEncryptedValue extends string,
  TAccountHostConfig extends string,
  TAccountZamaProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS,
>(
  input: MakeTokenAccountHandlePublicInput<
    TAccountPayer,
    TAccountOwner,
    TAccountMint,
    TAccountTokenAccount,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountZamaProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): MakeTokenAccountHandlePublicInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountOwner,
  TAccountMint,
  TAccountTokenAccount,
  TAccountEncryptedValue,
  TAccountHostConfig,
  TAccountZamaProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    tokenAccount: { value: input.tokenAccount ?? null, isWritable: false },
    encryptedValue: { value: input.encryptedValue ?? null, isWritable: true },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    zamaProgram: { value: input.zamaProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.zamaProgram.value) {
    accounts.zamaProgram.value =
      '6AtbvED1rfX68aCT1tYgU1aeu4kFksPDxZG9gtB1Fgtu' as Address<'6AtbvED1rfX68aCT1tYgU1aeu4kFksPDxZG9gtB1Fgtu'>;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('owner', accounts.owner),
      getAccountMeta('mint', accounts.mint),
      getAccountMeta('tokenAccount', accounts.tokenAccount),
      getAccountMeta('encryptedValue', accounts.encryptedValue),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('zamaProgram', accounts.zamaProgram),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getMakeTokenAccountHandlePublicInstructionDataEncoder().encode(
      args as MakeTokenAccountHandlePublicInstructionDataArgs,
    ),
    programAddress,
  } as MakeTokenAccountHandlePublicInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountMint,
    TAccountTokenAccount,
    TAccountEncryptedValue,
    TAccountHostConfig,
    TAccountZamaProgram,
    TAccountSystemProgram
  >);
}

export type ParsedMakeTokenAccountHandlePublicInstruction<
  TProgram extends string = typeof CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    owner: TAccountMetas[1];
    mint: TAccountMetas[2];
    tokenAccount: TAccountMetas[3];
    encryptedValue: TAccountMetas[4];
    hostConfig: TAccountMetas[5];
    zamaProgram: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: MakeTokenAccountHandlePublicInstructionData;
};

export function parseMakeTokenAccountHandlePublicInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedMakeTokenAccountHandlePublicInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 8,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      owner: getNextAccount(),
      mint: getNextAccount(),
      tokenAccount: getNextAccount(),
      encryptedValue: getNextAccount(),
      hostConfig: getNextAccount(),
      zamaProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMakeTokenAccountHandlePublicInstructionDataDecoder().decode(instruction.data),
  };
}
