/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from '@solana/kit';

/**
 * How a program receiving a transfer learns the amount it received: the transfer's execution
 * also writes `receipt = receipt + transferred` (first time `transferred + 0`) into a value of
 * that program, controlled by the PDA it signs the CPI with. A value's authority is the only
 * key that may compute on it, so the recipient cannot read `transferred_amount` itself; the
 * token program writes the sum into the recipient's own value instead, in the same execution
 * that computes it. EVM analog: `FHE.allow(transferred, recipient)` followed by the recipient's
 * own `add`.
 */
export type TransferReceipt = {
  /** The recipient program. */
  program: Address;
  /** The recipient program's scope for the value. */
  scope: ReadonlyUint8Array;
  /** The value's label under `(program, receipt_authority, scope)`. */
  label: ReadonlyUint8Array;
  /**
   * Seeds (bump last) deriving `receipt_authority` under `program`; proven by the host on
   * the first write.
   */
  authoritySeeds: Array<ReadonlyUint8Array>;
  /** Keys allowed to decrypt the receipt's new handle. */
  allows: Array<Address>;
};

export type TransferReceiptArgs = TransferReceipt;

export function getTransferReceiptEncoder(): Encoder<TransferReceiptArgs> {
  return getStructEncoder([
    ['program', getAddressEncoder()],
    ['scope', fixEncoderSize(getBytesEncoder(), 32)],
    ['label', fixEncoderSize(getBytesEncoder(), 32)],
    ['authoritySeeds', getArrayEncoder(addEncoderSizePrefix(getBytesEncoder(), getU32Encoder()))],
    ['allows', getArrayEncoder(getAddressEncoder())],
  ]);
}

export function getTransferReceiptDecoder(): Decoder<TransferReceipt> {
  return getStructDecoder([
    ['program', getAddressDecoder()],
    ['scope', fixDecoderSize(getBytesDecoder(), 32)],
    ['label', fixDecoderSize(getBytesDecoder(), 32)],
    ['authoritySeeds', getArrayDecoder(addDecoderSizePrefix(getBytesDecoder(), getU32Decoder()))],
    ['allows', getArrayDecoder(getAddressDecoder())],
  ]);
}

export function getTransferReceiptCodec(): Codec<TransferReceiptArgs, TransferReceipt> {
  return combineCodec(getTransferReceiptEncoder(), getTransferReceiptDecoder());
}
