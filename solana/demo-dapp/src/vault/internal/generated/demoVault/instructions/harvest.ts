/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { DEMO_VAULT_PROGRAM_ADDRESS } from '../programAddress.js';

export const HARVEST_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([228, 241, 31, 182, 53, 169, 59, 199]);

export function getHarvestDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(HARVEST_DISCRIMINATOR);
}

export type HarvestInstruction<
  TProgram extends string = typeof DEMO_VAULT_PROGRAM_ADDRESS,
  TAccountDonor extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountUnderlyingMint extends string | AccountMeta<string> = string,
  TAccountDonorUnderlying extends string | AccountMeta<string> = string,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDonor extends string
        ? WritableSignerAccount<TAccountDonor> & AccountSignerMeta<TAccountDonor>
        : TAccountDonor,
      TAccountVault extends string ? ReadonlyAccount<TAccountVault> : TAccountVault,
      TAccountUnderlyingMint extends string ? ReadonlyAccount<TAccountUnderlyingMint> : TAccountUnderlyingMint,
      TAccountDonorUnderlying extends string ? WritableAccount<TAccountDonorUnderlying> : TAccountDonorUnderlying,
      TAccountVaultTokenAccount extends string ? WritableAccount<TAccountVaultTokenAccount> : TAccountVaultTokenAccount,
      TAccountTokenProgram extends string ? ReadonlyAccount<TAccountTokenProgram> : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type HarvestInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type HarvestInstructionDataArgs = { amount: number | bigint };

export function getHarvestInstructionDataEncoder(): FixedSizeEncoder<HarvestInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['amount', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: HARVEST_DISCRIMINATOR }),
  );
}

export function getHarvestInstructionDataDecoder(): FixedSizeDecoder<HarvestInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['amount', getU64Decoder()],
  ]);
}

export function getHarvestInstructionDataCodec(): FixedSizeCodec<HarvestInstructionDataArgs, HarvestInstructionData> {
  return combineCodec(getHarvestInstructionDataEncoder(), getHarvestInstructionDataDecoder());
}

export type HarvestInput<
  TAccountDonor extends string = string,
  TAccountVault extends string = string,
  TAccountUnderlyingMint extends string = string,
  TAccountDonorUnderlying extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  /** Donor and transfer authority over `donor_underlying`. */
  donor: TransactionSigner<TAccountDonor>;
  /** Vault receiving the donation. */
  vault: Address<TAccountVault>;
  /** Underlying SPL mint, read for its decimals. */
  underlyingMint: Address<TAccountUnderlyingMint>;
  /** Donor's underlying token account (source). */
  donorUnderlying: Address<TAccountDonorUnderlying>;
  /** Vault token account whose balance is increased by the donation. */
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  /** SPL token program. */
  tokenProgram?: Address<TAccountTokenProgram>;
  amount: HarvestInstructionDataArgs['amount'];
};

export function getHarvestInstruction<
  TAccountDonor extends string,
  TAccountVault extends string,
  TAccountUnderlyingMint extends string,
  TAccountDonorUnderlying extends string,
  TAccountVaultTokenAccount extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof DEMO_VAULT_PROGRAM_ADDRESS,
>(
  input: HarvestInput<
    TAccountDonor,
    TAccountVault,
    TAccountUnderlyingMint,
    TAccountDonorUnderlying,
    TAccountVaultTokenAccount,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): HarvestInstruction<
  TProgramAddress,
  TAccountDonor,
  TAccountVault,
  TAccountUnderlyingMint,
  TAccountDonorUnderlying,
  TAccountVaultTokenAccount,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DEMO_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    donor: { value: input.donor ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: false },
    underlyingMint: { value: input.underlyingMint ?? null, isWritable: false },
    donorUnderlying: { value: input.donorUnderlying ?? null, isWritable: true },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('donor', accounts.donor),
      getAccountMeta('vault', accounts.vault),
      getAccountMeta('underlyingMint', accounts.underlyingMint),
      getAccountMeta('donorUnderlying', accounts.donorUnderlying),
      getAccountMeta('vaultTokenAccount', accounts.vaultTokenAccount),
      getAccountMeta('tokenProgram', accounts.tokenProgram),
    ],
    data: getHarvestInstructionDataEncoder().encode(args as HarvestInstructionDataArgs),
    programAddress,
  } as HarvestInstruction<
    TProgramAddress,
    TAccountDonor,
    TAccountVault,
    TAccountUnderlyingMint,
    TAccountDonorUnderlying,
    TAccountVaultTokenAccount,
    TAccountTokenProgram
  >);
}

export type ParsedHarvestInstruction<
  TProgram extends string = typeof DEMO_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Donor and transfer authority over `donor_underlying`. */
    donor: TAccountMetas[0];
    /** Vault receiving the donation. */
    vault: TAccountMetas[1];
    /** Underlying SPL mint, read for its decimals. */
    underlyingMint: TAccountMetas[2];
    /** Donor's underlying token account (source). */
    donorUnderlying: TAccountMetas[3];
    /** Vault token account whose balance is increased by the donation. */
    vaultTokenAccount: TAccountMetas[4];
    /** SPL token program. */
    tokenProgram: TAccountMetas[5];
  };
  data: HarvestInstructionData;
};

export function parseHarvestInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedHarvestInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 6,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      donor: getNextAccount(),
      vault: getNextAccount(),
      underlyingMint: getNextAccount(),
      donorUnderlying: getNextAccount(),
      vaultTokenAccount: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getHarvestInstructionDataDecoder().decode(instruction.data),
  };
}
