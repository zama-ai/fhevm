/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from '@solana/program-client-core';
import { findTokenAccountPda } from '../pdas/index.js';
import { CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS } from '../programAddress.js';

export const ALLOW_BALANCE_VIEWERS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  223, 184, 34, 52, 181, 16, 3, 168,
]);

export function getAllowBalanceViewersDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ALLOW_BALANCE_VIEWERS_DISCRIMINATOR);
}

export type AllowBalanceViewersInstruction<
  TProgram extends string = typeof CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountMint extends string | AccountMeta<string> = string,
  TAccountTokenAccount extends string | AccountMeta<string> = string,
  TAccountBalanceStore extends string | AccountMeta<string> = string,
  TAccountHostConfig extends string | AccountMeta<string> = string,
  TAccountZamaEventAuthority extends string | AccountMeta<string> = string,
  TAccountTransientStore extends string | AccountMeta<string> = string,
  TAccountInstructions extends string | AccountMeta<string> = string,
  TAccountZamaProgram extends string | AccountMeta<string> = 'DPq5y89RDZPq9NcMh9X1NgjBWgYmSXg3QoipSBV3ZMzQ',
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TAccountHcuBlockMeter extends string | AccountMeta<string> = string,
  TAccountHcuTrustedAppRecord extends string | AccountMeta<string> = string,
  TAccountEventAuthority extends string | AccountMeta<string> = string,
  TAccountProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountMint extends string ? ReadonlyAccount<TAccountMint> : TAccountMint,
      TAccountTokenAccount extends string ? ReadonlyAccount<TAccountTokenAccount> : TAccountTokenAccount,
      TAccountBalanceStore extends string ? WritableAccount<TAccountBalanceStore> : TAccountBalanceStore,
      TAccountHostConfig extends string ? ReadonlyAccount<TAccountHostConfig> : TAccountHostConfig,
      TAccountZamaEventAuthority extends string
        ? ReadonlyAccount<TAccountZamaEventAuthority>
        : TAccountZamaEventAuthority,
      TAccountTransientStore extends string ? WritableAccount<TAccountTransientStore> : TAccountTransientStore,
      TAccountInstructions extends string ? ReadonlyAccount<TAccountInstructions> : TAccountInstructions,
      TAccountZamaProgram extends string ? ReadonlyAccount<TAccountZamaProgram> : TAccountZamaProgram,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      TAccountHcuBlockMeter extends string ? WritableAccount<TAccountHcuBlockMeter> : TAccountHcuBlockMeter,
      TAccountHcuTrustedAppRecord extends string
        ? ReadonlyAccount<TAccountHcuTrustedAppRecord>
        : TAccountHcuTrustedAppRecord,
      TAccountEventAuthority extends string ? ReadonlyAccount<TAccountEventAuthority> : TAccountEventAuthority,
      TAccountProgram extends string ? ReadonlyAccount<TAccountProgram> : TAccountProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AllowBalanceViewersInstructionData = {
  discriminator: ReadonlyUint8Array;
  viewers: Array<Address>;
};

export type AllowBalanceViewersInstructionDataArgs = {
  viewers: Array<Address>;
};

export function getAllowBalanceViewersInstructionDataEncoder(): Encoder<AllowBalanceViewersInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['viewers', getArrayEncoder(getAddressEncoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: ALLOW_BALANCE_VIEWERS_DISCRIMINATOR,
    }),
  );
}

export function getAllowBalanceViewersInstructionDataDecoder(): Decoder<AllowBalanceViewersInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['viewers', getArrayDecoder(getAddressDecoder())],
  ]);
}

export function getAllowBalanceViewersInstructionDataCodec(): Codec<
  AllowBalanceViewersInstructionDataArgs,
  AllowBalanceViewersInstructionData
> {
  return combineCodec(getAllowBalanceViewersInstructionDataEncoder(), getAllowBalanceViewersInstructionDataDecoder());
}

export type AllowBalanceViewersAsyncInput<
  TAccountPayer extends string = string,
  TAccountOwner extends string = string,
  TAccountMint extends string = string,
  TAccountTokenAccount extends string = string,
  TAccountBalanceStore extends string = string,
  TAccountHostConfig extends string = string,
  TAccountZamaEventAuthority extends string = string,
  TAccountTransientStore extends string = string,
  TAccountInstructions extends string = string,
  TAccountZamaProgram extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountHcuBlockMeter extends string = string,
  TAccountHcuTrustedAppRecord extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** Pays for encrypted store growth on the host. */
  payer: TransactionSigner<TAccountPayer>;
  /** Token account owner authorizing the grant. */
  owner: TransactionSigner<TAccountOwner>;
  mint: Address<TAccountMint>;
  tokenAccount?: Address<TAccountTokenAccount>;
  /** Stable balance encrypted store; read for the current handle and replaced. */
  balanceStore: Address<TAccountBalanceStore>;
  hostConfig: Address<TAccountHostConfig>;
  zamaEventAuthority: Address<TAccountZamaEventAuthority>;
  transientStore: Address<TAccountTransientStore>;
  instructions: Address<TAccountInstructions>;
  zamaProgram?: Address<TAccountZamaProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  /**
   * canonical `["hcu-block-meter", program, mint]` PDA. Supplied by an untrusted mint under a
   * metering-band cap; omitted when the mint is trusted or the cap is unrestricted.
   */
  hcuBlockMeter?: Address<TAccountHcuBlockMeter>;
  /**
   * canonical `["hcu-trusted", program, mint]` PDA. Present + valid bypasses the cap; absent
   * means the mint is metered.
   */
  hcuTrustedAppRecord?: Address<TAccountHcuTrustedAppRecord>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  viewers: AllowBalanceViewersInstructionDataArgs['viewers'];
};

export async function getAllowBalanceViewersInstructionAsync<
  TAccountPayer extends string,
  TAccountOwner extends string,
  TAccountMint extends string,
  TAccountTokenAccount extends string,
  TAccountBalanceStore extends string,
  TAccountHostConfig extends string,
  TAccountZamaEventAuthority extends string,
  TAccountTransientStore extends string,
  TAccountInstructions extends string,
  TAccountZamaProgram extends string,
  TAccountSystemProgram extends string,
  TAccountHcuBlockMeter extends string,
  TAccountHcuTrustedAppRecord extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS,
>(
  input: AllowBalanceViewersAsyncInput<
    TAccountPayer,
    TAccountOwner,
    TAccountMint,
    TAccountTokenAccount,
    TAccountBalanceStore,
    TAccountHostConfig,
    TAccountZamaEventAuthority,
    TAccountTransientStore,
    TAccountInstructions,
    TAccountZamaProgram,
    TAccountSystemProgram,
    TAccountHcuBlockMeter,
    TAccountHcuTrustedAppRecord,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AllowBalanceViewersInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountMint,
    TAccountTokenAccount,
    TAccountBalanceStore,
    TAccountHostConfig,
    TAccountZamaEventAuthority,
    TAccountTransientStore,
    TAccountInstructions,
    TAccountZamaProgram,
    TAccountSystemProgram,
    TAccountHcuBlockMeter,
    TAccountHcuTrustedAppRecord,
    TAccountEventAuthority,
    TAccountProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    tokenAccount: { value: input.tokenAccount ?? null, isWritable: false },
    balanceStore: { value: input.balanceStore ?? null, isWritable: true },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    zamaEventAuthority: {
      value: input.zamaEventAuthority ?? null,
      isWritable: false,
    },
    transientStore: { value: input.transientStore ?? null, isWritable: true },
    instructions: { value: input.instructions ?? null, isWritable: false },
    zamaProgram: { value: input.zamaProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    hcuBlockMeter: { value: input.hcuBlockMeter ?? null, isWritable: true },
    hcuTrustedAppRecord: {
      value: input.hcuTrustedAppRecord ?? null,
      isWritable: false,
    },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenAccount.value) {
    accounts.tokenAccount.value = await findTokenAccountPda({
      mint: getAddressFromResolvedInstructionAccount('mint', accounts.mint.value),
      owner: getAddressFromResolvedInstructionAccount('owner', accounts.owner.value),
    });
  }
  if (!accounts.zamaProgram.value) {
    accounts.zamaProgram.value =
      'DPq5y89RDZPq9NcMh9X1NgjBWgYmSXg3QoipSBV3ZMzQ' as Address<'DPq5y89RDZPq9NcMh9X1NgjBWgYmSXg3QoipSBV3ZMzQ'>;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('owner', accounts.owner),
      getAccountMeta('mint', accounts.mint),
      getAccountMeta('tokenAccount', accounts.tokenAccount),
      getAccountMeta('balanceStore', accounts.balanceStore),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('zamaEventAuthority', accounts.zamaEventAuthority),
      getAccountMeta('transientStore', accounts.transientStore),
      getAccountMeta('instructions', accounts.instructions),
      getAccountMeta('zamaProgram', accounts.zamaProgram),
      getAccountMeta('systemProgram', accounts.systemProgram),
      getAccountMeta('hcuBlockMeter', accounts.hcuBlockMeter),
      getAccountMeta('hcuTrustedAppRecord', accounts.hcuTrustedAppRecord),
      getAccountMeta('eventAuthority', accounts.eventAuthority),
      getAccountMeta('program', accounts.program),
    ],
    data: getAllowBalanceViewersInstructionDataEncoder().encode(args as AllowBalanceViewersInstructionDataArgs),
    programAddress,
  } as AllowBalanceViewersInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountMint,
    TAccountTokenAccount,
    TAccountBalanceStore,
    TAccountHostConfig,
    TAccountZamaEventAuthority,
    TAccountTransientStore,
    TAccountInstructions,
    TAccountZamaProgram,
    TAccountSystemProgram,
    TAccountHcuBlockMeter,
    TAccountHcuTrustedAppRecord,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type AllowBalanceViewersInput<
  TAccountPayer extends string = string,
  TAccountOwner extends string = string,
  TAccountMint extends string = string,
  TAccountTokenAccount extends string = string,
  TAccountBalanceStore extends string = string,
  TAccountHostConfig extends string = string,
  TAccountZamaEventAuthority extends string = string,
  TAccountTransientStore extends string = string,
  TAccountInstructions extends string = string,
  TAccountZamaProgram extends string = string,
  TAccountSystemProgram extends string = string,
  TAccountHcuBlockMeter extends string = string,
  TAccountHcuTrustedAppRecord extends string = string,
  TAccountEventAuthority extends string = string,
  TAccountProgram extends string = string,
> = {
  /** Pays for encrypted store growth on the host. */
  payer: TransactionSigner<TAccountPayer>;
  /** Token account owner authorizing the grant. */
  owner: TransactionSigner<TAccountOwner>;
  mint: Address<TAccountMint>;
  tokenAccount: Address<TAccountTokenAccount>;
  /** Stable balance encrypted store; read for the current handle and replaced. */
  balanceStore: Address<TAccountBalanceStore>;
  hostConfig: Address<TAccountHostConfig>;
  zamaEventAuthority: Address<TAccountZamaEventAuthority>;
  transientStore: Address<TAccountTransientStore>;
  instructions: Address<TAccountInstructions>;
  zamaProgram?: Address<TAccountZamaProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  /**
   * canonical `["hcu-block-meter", program, mint]` PDA. Supplied by an untrusted mint under a
   * metering-band cap; omitted when the mint is trusted or the cap is unrestricted.
   */
  hcuBlockMeter?: Address<TAccountHcuBlockMeter>;
  /**
   * canonical `["hcu-trusted", program, mint]` PDA. Present + valid bypasses the cap; absent
   * means the mint is metered.
   */
  hcuTrustedAppRecord?: Address<TAccountHcuTrustedAppRecord>;
  eventAuthority: Address<TAccountEventAuthority>;
  program: Address<TAccountProgram>;
  viewers: AllowBalanceViewersInstructionDataArgs['viewers'];
};

export function getAllowBalanceViewersInstruction<
  TAccountPayer extends string,
  TAccountOwner extends string,
  TAccountMint extends string,
  TAccountTokenAccount extends string,
  TAccountBalanceStore extends string,
  TAccountHostConfig extends string,
  TAccountZamaEventAuthority extends string,
  TAccountTransientStore extends string,
  TAccountInstructions extends string,
  TAccountZamaProgram extends string,
  TAccountSystemProgram extends string,
  TAccountHcuBlockMeter extends string,
  TAccountHcuTrustedAppRecord extends string,
  TAccountEventAuthority extends string,
  TAccountProgram extends string,
  TProgramAddress extends Address = typeof CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS,
>(
  input: AllowBalanceViewersInput<
    TAccountPayer,
    TAccountOwner,
    TAccountMint,
    TAccountTokenAccount,
    TAccountBalanceStore,
    TAccountHostConfig,
    TAccountZamaEventAuthority,
    TAccountTransientStore,
    TAccountInstructions,
    TAccountZamaProgram,
    TAccountSystemProgram,
    TAccountHcuBlockMeter,
    TAccountHcuTrustedAppRecord,
    TAccountEventAuthority,
    TAccountProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AllowBalanceViewersInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountOwner,
  TAccountMint,
  TAccountTokenAccount,
  TAccountBalanceStore,
  TAccountHostConfig,
  TAccountZamaEventAuthority,
  TAccountTransientStore,
  TAccountInstructions,
  TAccountZamaProgram,
  TAccountSystemProgram,
  TAccountHcuBlockMeter,
  TAccountHcuTrustedAppRecord,
  TAccountEventAuthority,
  TAccountProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    mint: { value: input.mint ?? null, isWritable: false },
    tokenAccount: { value: input.tokenAccount ?? null, isWritable: false },
    balanceStore: { value: input.balanceStore ?? null, isWritable: true },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    zamaEventAuthority: {
      value: input.zamaEventAuthority ?? null,
      isWritable: false,
    },
    transientStore: { value: input.transientStore ?? null, isWritable: true },
    instructions: { value: input.instructions ?? null, isWritable: false },
    zamaProgram: { value: input.zamaProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    hcuBlockMeter: { value: input.hcuBlockMeter ?? null, isWritable: true },
    hcuTrustedAppRecord: {
      value: input.hcuTrustedAppRecord ?? null,
      isWritable: false,
    },
    eventAuthority: { value: input.eventAuthority ?? null, isWritable: false },
    program: { value: input.program ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.zamaProgram.value) {
    accounts.zamaProgram.value =
      'DPq5y89RDZPq9NcMh9X1NgjBWgYmSXg3QoipSBV3ZMzQ' as Address<'DPq5y89RDZPq9NcMh9X1NgjBWgYmSXg3QoipSBV3ZMzQ'>;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('owner', accounts.owner),
      getAccountMeta('mint', accounts.mint),
      getAccountMeta('tokenAccount', accounts.tokenAccount),
      getAccountMeta('balanceStore', accounts.balanceStore),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('zamaEventAuthority', accounts.zamaEventAuthority),
      getAccountMeta('transientStore', accounts.transientStore),
      getAccountMeta('instructions', accounts.instructions),
      getAccountMeta('zamaProgram', accounts.zamaProgram),
      getAccountMeta('systemProgram', accounts.systemProgram),
      getAccountMeta('hcuBlockMeter', accounts.hcuBlockMeter),
      getAccountMeta('hcuTrustedAppRecord', accounts.hcuTrustedAppRecord),
      getAccountMeta('eventAuthority', accounts.eventAuthority),
      getAccountMeta('program', accounts.program),
    ],
    data: getAllowBalanceViewersInstructionDataEncoder().encode(args as AllowBalanceViewersInstructionDataArgs),
    programAddress,
  } as AllowBalanceViewersInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountOwner,
    TAccountMint,
    TAccountTokenAccount,
    TAccountBalanceStore,
    TAccountHostConfig,
    TAccountZamaEventAuthority,
    TAccountTransientStore,
    TAccountInstructions,
    TAccountZamaProgram,
    TAccountSystemProgram,
    TAccountHcuBlockMeter,
    TAccountHcuTrustedAppRecord,
    TAccountEventAuthority,
    TAccountProgram
  >);
}

export type ParsedAllowBalanceViewersInstruction<
  TProgram extends string = typeof CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays for encrypted store growth on the host. */
    payer: TAccountMetas[0];
    /** Token account owner authorizing the grant. */
    owner: TAccountMetas[1];
    mint: TAccountMetas[2];
    tokenAccount: TAccountMetas[3];
    /** Stable balance encrypted store; read for the current handle and replaced. */
    balanceStore: TAccountMetas[4];
    hostConfig: TAccountMetas[5];
    zamaEventAuthority: TAccountMetas[6];
    transientStore: TAccountMetas[7];
    instructions: TAccountMetas[8];
    zamaProgram: TAccountMetas[9];
    systemProgram: TAccountMetas[10];
    /**
     * canonical `["hcu-block-meter", program, mint]` PDA. Supplied by an untrusted mint under a
     * metering-band cap; omitted when the mint is trusted or the cap is unrestricted.
     */
    hcuBlockMeter?: TAccountMetas[11] | undefined;
    /**
     * canonical `["hcu-trusted", program, mint]` PDA. Present + valid bypasses the cap; absent
     * means the mint is metered.
     */
    hcuTrustedAppRecord?: TAccountMetas[12] | undefined;
    eventAuthority: TAccountMetas[13];
    program: TAccountMetas[14];
  };
  data: AllowBalanceViewersInstructionData;
};

export function parseAllowBalanceViewersInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedAllowBalanceViewersInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 15) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 15,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === CONFIDENTIAL_TOKEN_PROGRAM_ADDRESS ? undefined : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      owner: getNextAccount(),
      mint: getNextAccount(),
      tokenAccount: getNextAccount(),
      balanceStore: getNextAccount(),
      hostConfig: getNextAccount(),
      zamaEventAuthority: getNextAccount(),
      transientStore: getNextAccount(),
      instructions: getNextAccount(),
      zamaProgram: getNextAccount(),
      systemProgram: getNextAccount(),
      hcuBlockMeter: getNextOptionalAccount(),
      hcuTrustedAppRecord: getNextOptionalAccount(),
      eventAuthority: getNextAccount(),
      program: getNextAccount(),
    },
    data: getAllowBalanceViewersInstructionDataDecoder().decode(instruction.data),
  };
}
