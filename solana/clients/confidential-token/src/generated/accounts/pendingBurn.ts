/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const PENDING_BURN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([203, 79, 129, 4, 87, 94, 246, 250]);

export function getPendingBurnDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PENDING_BURN_DISCRIMINATOR);
}

export type PendingBurn = {
  discriminator: ReadonlyUint8Array;
  /** Confidential mint whose underlying-token vault funds a redemption payout. */
  mint: Address;
  /** Token account owner (rent destination on close). */
  owner: Address;
  /** Token account that produced the burned amount. */
  tokenAccount: Address;
  /** Burned amount handle produced by the burn execution (stored for redeem matching). */
  burnedHandle: ReadonlyUint8Array;
  /** PDA bump for `(mint, token_account)`. */
  bump: number;
};

export type PendingBurnArgs = {
  /** Confidential mint whose underlying-token vault funds a redemption payout. */
  mint: Address;
  /** Token account owner (rent destination on close). */
  owner: Address;
  /** Token account that produced the burned amount. */
  tokenAccount: Address;
  /** Burned amount handle produced by the burn execution (stored for redeem matching). */
  burnedHandle: ReadonlyUint8Array;
  /** PDA bump for `(mint, token_account)`. */
  bump: number;
};

/** Gets the encoder for {@link PendingBurnArgs} account data. */
export function getPendingBurnEncoder(): FixedSizeEncoder<PendingBurnArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['mint', getAddressEncoder()],
      ['owner', getAddressEncoder()],
      ['tokenAccount', getAddressEncoder()],
      ['burnedHandle', fixEncoderSize(getBytesEncoder(), 32)],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: PENDING_BURN_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link PendingBurn} account data. */
export function getPendingBurnDecoder(): FixedSizeDecoder<PendingBurn> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['mint', getAddressDecoder()],
    ['owner', getAddressDecoder()],
    ['tokenAccount', getAddressDecoder()],
    ['burnedHandle', fixDecoderSize(getBytesDecoder(), 32)],
    ['bump', getU8Decoder()],
  ]);
}

/** Gets the codec for {@link PendingBurn} account data. */
export function getPendingBurnCodec(): FixedSizeCodec<PendingBurnArgs, PendingBurn> {
  return combineCodec(getPendingBurnEncoder(), getPendingBurnDecoder());
}

export function decodePendingBurn<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<PendingBurn, TAddress>;
export function decodePendingBurn<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<PendingBurn, TAddress>;
export function decodePendingBurn<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<PendingBurn, TAddress> | MaybeAccount<PendingBurn, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getPendingBurnDecoder());
}

export async function fetchPendingBurn<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<PendingBurn, TAddress>> {
  const maybeAccount = await fetchMaybePendingBurn(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybePendingBurn<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<PendingBurn, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodePendingBurn(maybeAccount);
}

export async function fetchAllPendingBurn(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<PendingBurn>[]> {
  const maybeAccounts = await fetchAllMaybePendingBurn(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybePendingBurn(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<PendingBurn>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodePendingBurn(maybeAccount));
}

export function getPendingBurnSize(): number {
  return 137;
}
