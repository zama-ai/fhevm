/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const CONFIDENTIAL_TOKEN_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  162, 241, 85, 134, 20, 132, 103, 199,
]);

export function getConfidentialTokenAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CONFIDENTIAL_TOKEN_ACCOUNT_DISCRIMINATOR);
}

export type ConfidentialTokenAccount = {
  discriminator: ReadonlyUint8Array;
  /** Token account owner. */
  owner: Address;
  /** Confidential mint this account belongs to. */
  mint: Address;
  /** PDA bump for the token account. */
  bump: number;
};

export type ConfidentialTokenAccountArgs = {
  /** Token account owner. */
  owner: Address;
  /** Confidential mint this account belongs to. */
  mint: Address;
  /** PDA bump for the token account. */
  bump: number;
};

/** Gets the encoder for {@link ConfidentialTokenAccountArgs} account data. */
export function getConfidentialTokenAccountEncoder(): FixedSizeEncoder<ConfidentialTokenAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['owner', getAddressEncoder()],
      ['mint', getAddressEncoder()],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CONFIDENTIAL_TOKEN_ACCOUNT_DISCRIMINATOR,
    }),
  );
}

/** Gets the decoder for {@link ConfidentialTokenAccount} account data. */
export function getConfidentialTokenAccountDecoder(): FixedSizeDecoder<ConfidentialTokenAccount> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['owner', getAddressDecoder()],
    ['mint', getAddressDecoder()],
    ['bump', getU8Decoder()],
  ]);
}

/** Gets the codec for {@link ConfidentialTokenAccount} account data. */
export function getConfidentialTokenAccountCodec(): FixedSizeCodec<
  ConfidentialTokenAccountArgs,
  ConfidentialTokenAccount
> {
  return combineCodec(getConfidentialTokenAccountEncoder(), getConfidentialTokenAccountDecoder());
}

export function decodeConfidentialTokenAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ConfidentialTokenAccount, TAddress>;
export function decodeConfidentialTokenAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ConfidentialTokenAccount, TAddress>;
export function decodeConfidentialTokenAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<ConfidentialTokenAccount, TAddress> | MaybeAccount<ConfidentialTokenAccount, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getConfidentialTokenAccountDecoder());
}

export async function fetchConfidentialTokenAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ConfidentialTokenAccount, TAddress>> {
  const maybeAccount = await fetchMaybeConfidentialTokenAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeConfidentialTokenAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ConfidentialTokenAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeConfidentialTokenAccount(maybeAccount);
}

export async function fetchAllConfidentialTokenAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ConfidentialTokenAccount>[]> {
  const maybeAccounts = await fetchAllMaybeConfidentialTokenAccount(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeConfidentialTokenAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ConfidentialTokenAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeConfidentialTokenAccount(maybeAccount));
}

export function getConfidentialTokenAccountSize(): number {
  return 73;
}
