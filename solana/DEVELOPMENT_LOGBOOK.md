# Solana PoC Development Logbook

The development logbook has been distilled into durable docs:

- `docs/DEVELOPMENT_HISTORY.md` — how the PoC was built (the two chapters), the hardening catalog,
  and where things stand.
- `docs/TESTING.md` — how to run the suites, Mollusk vs LiteSVM, and the build/dev traps (stale
  SBF, `anchor build` program-id sync, `SQLX_OFFLINE`, sequential cargo, etc.).
