# Transient Allow On Solana

This permanent design note moved to [`docs/TRANSIENT_ALLOW.md`](docs/TRANSIENT_ALLOW.md).

Keep durable rationale in `solana/docs/`. This root file remains only as a compatibility pointer
for older references.
