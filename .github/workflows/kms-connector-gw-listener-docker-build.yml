name: kms-connector-gw-listener-docker-build

on:
  pull_request:
  push:
    branches:
      - main
  release:
    types:
      - published

concurrency:
  group: fhevm-kms-connector-gw-listener-${{ github.ref_name }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  build:
    name: kms-connector-gw-listener-docker-build/build
    if: >-
      (startsWith(github.ref, 'refs/heads/release/') || github.ref == 'refs/heads/main') ||
      (github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'docker'))
    uses: zama-ai/ci-templates/.github/workflows/common-docker.yml@6323a1e8e260961562b66a9f33a608f86cd6c831
    secrets:
      BLOCKCHAIN_ACTIONS_TOKEN: ${{ secrets.BLOCKCHAIN_ACTIONS_TOKEN }}
      AWS_ACCESS_KEY_S3_USER: ${{ secrets.AWS_ACCESS_KEY_S3_USER }}
      AWS_SECRET_KEY_S3_USER: ${{ secrets.AWS_SECRET_KEY_S3_USER }}
    permissions:
      actions: 'read'
      contents: 'read'
      pull-requests: 'read'
      attestations: write
      packages: 'write'
      id-token: 'write'
    with:
      working-directory: "."
      docker-context: "."
      push_image: true
      image-name: "fhevm/kms-connector/gw-listener"
      docker-file: "./kms-connector/crates/gw-listener/Dockerfile"
      app-cache-dir: "fhevm-kms-connector-gw-listener"
