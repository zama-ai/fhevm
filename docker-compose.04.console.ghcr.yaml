# Names to target built images
services:
  back:
    image: ghcr.io/zama-ai/console/back:${DOCKER_TAG:-latest}

  front:
    image: ghcr.io/zama-ai/console/front:${DOCKER_TAG:-latest}

  relayer:
    image: ghcr.io/zama-ai/console/relayer:${DOCKER_TAG:-latest}
