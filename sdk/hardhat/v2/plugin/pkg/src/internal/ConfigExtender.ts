import type { ConfigExtender, HardhatConfig, HardhatUserConfig } from 'hardhat/types';

export const configExtender: ConfigExtender = (_config: HardhatConfig, _userConfig: Readonly<HardhatUserConfig>) => {
  /* empty */
};
