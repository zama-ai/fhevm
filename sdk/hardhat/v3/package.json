{
  "name": "fhevm-hardhat-v3-workspace",
  "version": "0.0.0",
  "description": "Installation root for the hardhat v3 generation: one npm workspace, so every member resolves the SAME hardhat@3 by hoisting — the no-symlink singleton guarantee.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "plugin",
    "plugin/pkg",
    "fhevm-hardhat-template",
    "fhevm-hardhat-template/pkg",
    "e2e"
  ]
}
