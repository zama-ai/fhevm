{
  "name": "fhevm-hardhat-v3-workspace",
  "version": "0.0.0",
  "description": "Installation root for the hardhat v3 generation: one npm workspace, so every member resolves the SAME hardhat@3 by hoisting — the no-symlink singleton guarantee. The fhevm-hardhat-template sibling joins this workspaces list when it lands.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "plugin",
    "plugin/pkg",
    "e2e"
  ]
}
