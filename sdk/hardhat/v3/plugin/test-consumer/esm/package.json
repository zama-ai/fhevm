{
  "name": "hardhat-plugin-v3-consumer-esm",
  "version": "0.0.0",
  "description": "Isolated ESM consumer of @fhevm/hardhat-plugin (hardhat v3): installs the payload the way a user would and proves the plugin loads.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test --test-concurrency=1 src/*.test.js"
  },
  "devDependencies": {
    "@fhevm/hardhat-plugin": "file:../../pkg",
    "hardhat": "^3.0.0"
  }
}
