import { type PrepareTestConsumerOptions, prepareTestConsumer } from '../base/test-consumer.ts';

export function testConsumer(options: PrepareTestConsumerOptions): void {
  prepareTestConsumer(options);
}
