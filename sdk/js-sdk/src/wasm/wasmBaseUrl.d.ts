/**
 * Base URL for resolving WASM file paths relative to this module.
 * Works in both ESM and CJS contexts.
 *
 * Paths must be relative to `src/wasm/`.
 */
export declare const wasmBaseUrl: string;
