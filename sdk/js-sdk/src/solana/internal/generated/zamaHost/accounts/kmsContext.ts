/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getKmsThresholdsDecoder,
  getKmsThresholdsEncoder,
  type KmsThresholds,
  type KmsThresholdsArgs,
} from '../types/index.js';

export const KMS_CONTEXT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([93, 0, 205, 40, 74, 63, 201, 149]);

export function getKmsContextDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(KMS_CONTEXT_DISCRIMINATOR);
}

export type KmsContext = {
  discriminator: ReadonlyUint8Array;
  /** Opaque 32-byte context id (mirrors EVM `kmsContextId`). All-zero is reserved (none defined). */
  contextId: ReadonlyUint8Array;
  /** KMS node signer EVM addresses authorized to sign certs in this context. */
  signers: Array<ReadonlyUint8Array>;
  /** Per-operation signature thresholds. */
  thresholds: KmsThresholds;
  /** True once destroyed (mirrors `destroyKmsContext`); destroyed contexts reject. */
  destroyed: boolean;
  /** PDA bump for `PDA("kms-context", context_id)`. */
  bump: number;
};

export type KmsContextArgs = {
  /** Opaque 32-byte context id (mirrors EVM `kmsContextId`). All-zero is reserved (none defined). */
  contextId: ReadonlyUint8Array;
  /** KMS node signer EVM addresses authorized to sign certs in this context. */
  signers: Array<ReadonlyUint8Array>;
  /** Per-operation signature thresholds. */
  thresholds: KmsThresholdsArgs;
  /** True once destroyed (mirrors `destroyKmsContext`); destroyed contexts reject. */
  destroyed: boolean;
  /** PDA bump for `PDA("kms-context", context_id)`. */
  bump: number;
};

/** Gets the encoder for {@link KmsContextArgs} account data. */
export function getKmsContextEncoder(): Encoder<KmsContextArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['contextId', fixEncoderSize(getBytesEncoder(), 32)],
      ['signers', getArrayEncoder(fixEncoderSize(getBytesEncoder(), 20))],
      ['thresholds', getKmsThresholdsEncoder()],
      ['destroyed', getBooleanEncoder()],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: KMS_CONTEXT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link KmsContext} account data. */
export function getKmsContextDecoder(): Decoder<KmsContext> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['contextId', fixDecoderSize(getBytesDecoder(), 32)],
    ['signers', getArrayDecoder(fixDecoderSize(getBytesDecoder(), 20))],
    ['thresholds', getKmsThresholdsDecoder()],
    ['destroyed', getBooleanDecoder()],
    ['bump', getU8Decoder()],
  ]);
}

/** Gets the codec for {@link KmsContext} account data. */
export function getKmsContextCodec(): Codec<KmsContextArgs, KmsContext> {
  return combineCodec(getKmsContextEncoder(), getKmsContextDecoder());
}

export function decodeKmsContext<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<KmsContext, TAddress>;
export function decodeKmsContext<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<KmsContext, TAddress>;
export function decodeKmsContext<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<KmsContext, TAddress> | MaybeAccount<KmsContext, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getKmsContextDecoder());
}

export async function fetchKmsContext<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<KmsContext, TAddress>> {
  const maybeAccount = await fetchMaybeKmsContext(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeKmsContext<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<KmsContext, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeKmsContext(maybeAccount);
}

export async function fetchAllKmsContext(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<KmsContext>[]> {
  const maybeAccounts = await fetchAllMaybeKmsContext(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeKmsContext(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<KmsContext>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeKmsContext(maybeAccount));
}
