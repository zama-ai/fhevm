/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { ZAMA_HOST_PROGRAM_ADDRESS } from '../programAddress.js';

export const REVOKE_PERMITS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([51, 25, 89, 125, 125, 90, 200, 130]);

export function getRevokePermitsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REVOKE_PERMITS_DISCRIMINATOR);
}

export type RevokePermitsInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountInvalidation extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountInvalidation extends string ? WritableAccount<TAccountInvalidation> : TAccountInvalidation,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RevokePermitsInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RevokePermitsInstructionDataArgs = {};

export function getRevokePermitsInstructionDataEncoder(): FixedSizeEncoder<RevokePermitsInstructionDataArgs> {
  return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({
    ...value,
    discriminator: REVOKE_PERMITS_DISCRIMINATOR,
  }));
}

export function getRevokePermitsInstructionDataDecoder(): FixedSizeDecoder<RevokePermitsInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getRevokePermitsInstructionDataCodec(): FixedSizeCodec<
  RevokePermitsInstructionDataArgs,
  RevokePermitsInstructionData
> {
  return combineCodec(getRevokePermitsInstructionDataEncoder(), getRevokePermitsInstructionDataDecoder());
}

export type RevokePermitsInput<
  TAccountUser extends string = string,
  TAccountInvalidation extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** The user revoking their permits, and the payer for the watermark account. */
  user: TransactionSigner<TAccountUser>;
  /** then created if absent. */
  invalidation: Address<TAccountInvalidation>;
  /** System program, used when the watermark account has to be created. */
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRevokePermitsInstruction<
  TAccountUser extends string,
  TAccountInvalidation extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: RevokePermitsInput<TAccountUser, TAccountInvalidation, TAccountSystemProgram>,
  config?: { programAddress?: TProgramAddress },
): RevokePermitsInstruction<TProgramAddress, TAccountUser, TAccountInvalidation, TAccountSystemProgram> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    invalidation: { value: input.invalidation ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('user', accounts.user),
      getAccountMeta('invalidation', accounts.invalidation),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getRevokePermitsInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokePermitsInstruction<TProgramAddress, TAccountUser, TAccountInvalidation, TAccountSystemProgram>);
}

export type ParsedRevokePermitsInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** The user revoking their permits, and the payer for the watermark account. */
    user: TAccountMetas[0];
    /** then created if absent. */
    invalidation: TAccountMetas[1];
    /** System program, used when the watermark account has to be created. */
    systemProgram: TAccountMetas[2];
  };
  data: RevokePermitsInstructionData;
};

export function parseRevokePermitsInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokePermitsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      invalidation: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRevokePermitsInstructionDataDecoder().decode(instruction.data),
  };
}
