/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findHostConfigPda } from '../pdas/index.js';
import { ZAMA_HOST_PROGRAM_ADDRESS } from '../programAddress.js';

export const REVOKE_DELEGATION_FOR_USER_DECRYPTION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  147, 27, 126, 53, 65, 37, 118, 225,
]);

export function getRevokeDelegationForUserDecryptionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REVOKE_DELEGATION_FOR_USER_DECRYPTION_DISCRIMINATOR);
}

export type RevokeDelegationForUserDecryptionInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountDelegator extends string | AccountMeta<string> = string,
  TAccountHostConfig extends string | AccountMeta<string> = string,
  TAccountDelegationRecord extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountDelegator extends string
        ? ReadonlySignerAccount<TAccountDelegator> & AccountSignerMeta<TAccountDelegator>
        : TAccountDelegator,
      TAccountHostConfig extends string ? ReadonlyAccount<TAccountHostConfig> : TAccountHostConfig,
      TAccountDelegationRecord extends string ? WritableAccount<TAccountDelegationRecord> : TAccountDelegationRecord,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeDelegationForUserDecryptionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RevokeDelegationForUserDecryptionInstructionDataArgs = {};

export function getRevokeDelegationForUserDecryptionInstructionDataEncoder(): FixedSizeEncoder<RevokeDelegationForUserDecryptionInstructionDataArgs> {
  return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({
    ...value,
    discriminator: REVOKE_DELEGATION_FOR_USER_DECRYPTION_DISCRIMINATOR,
  }));
}

export function getRevokeDelegationForUserDecryptionInstructionDataDecoder(): FixedSizeDecoder<RevokeDelegationForUserDecryptionInstructionData> {
  return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getRevokeDelegationForUserDecryptionInstructionDataCodec(): FixedSizeCodec<
  RevokeDelegationForUserDecryptionInstructionDataArgs,
  RevokeDelegationForUserDecryptionInstructionData
> {
  return combineCodec(
    getRevokeDelegationForUserDecryptionInstructionDataEncoder(),
    getRevokeDelegationForUserDecryptionInstructionDataDecoder(),
  );
}

export type RevokeDelegationForUserDecryptionAsyncInput<
  TAccountDelegator extends string = string,
  TAccountHostConfig extends string = string,
  TAccountDelegationRecord extends string = string,
> = {
  /** Delegator that owns the delegation. */
  delegator: TransactionSigner<TAccountDelegator>;
  /** Singleton config PDA. */
  hostConfig?: Address<TAccountHostConfig>;
  /** Delegation record to revoke. */
  delegationRecord: Address<TAccountDelegationRecord>;
};

export async function getRevokeDelegationForUserDecryptionInstructionAsync<
  TAccountDelegator extends string,
  TAccountHostConfig extends string,
  TAccountDelegationRecord extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: RevokeDelegationForUserDecryptionAsyncInput<TAccountDelegator, TAccountHostConfig, TAccountDelegationRecord>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RevokeDelegationForUserDecryptionInstruction<
    TProgramAddress,
    TAccountDelegator,
    TAccountHostConfig,
    TAccountDelegationRecord
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    delegator: { value: input.delegator ?? null, isWritable: false },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    delegationRecord: {
      value: input.delegationRecord ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Resolve default values.
  if (!accounts.hostConfig.value) {
    accounts.hostConfig.value = await findHostConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('delegator', accounts.delegator),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('delegationRecord', accounts.delegationRecord),
    ],
    data: getRevokeDelegationForUserDecryptionInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeDelegationForUserDecryptionInstruction<
    TProgramAddress,
    TAccountDelegator,
    TAccountHostConfig,
    TAccountDelegationRecord
  >);
}

export type RevokeDelegationForUserDecryptionInput<
  TAccountDelegator extends string = string,
  TAccountHostConfig extends string = string,
  TAccountDelegationRecord extends string = string,
> = {
  /** Delegator that owns the delegation. */
  delegator: TransactionSigner<TAccountDelegator>;
  /** Singleton config PDA. */
  hostConfig: Address<TAccountHostConfig>;
  /** Delegation record to revoke. */
  delegationRecord: Address<TAccountDelegationRecord>;
};

export function getRevokeDelegationForUserDecryptionInstruction<
  TAccountDelegator extends string,
  TAccountHostConfig extends string,
  TAccountDelegationRecord extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: RevokeDelegationForUserDecryptionInput<TAccountDelegator, TAccountHostConfig, TAccountDelegationRecord>,
  config?: { programAddress?: TProgramAddress },
): RevokeDelegationForUserDecryptionInstruction<
  TProgramAddress,
  TAccountDelegator,
  TAccountHostConfig,
  TAccountDelegationRecord
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    delegator: { value: input.delegator ?? null, isWritable: false },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    delegationRecord: {
      value: input.delegationRecord ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('delegator', accounts.delegator),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('delegationRecord', accounts.delegationRecord),
    ],
    data: getRevokeDelegationForUserDecryptionInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeDelegationForUserDecryptionInstruction<
    TProgramAddress,
    TAccountDelegator,
    TAccountHostConfig,
    TAccountDelegationRecord
  >);
}

export type ParsedRevokeDelegationForUserDecryptionInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Delegator that owns the delegation. */
    delegator: TAccountMetas[0];
    /** Singleton config PDA. */
    hostConfig: TAccountMetas[1];
    /** Delegation record to revoke. */
    delegationRecord: TAccountMetas[2];
  };
  data: RevokeDelegationForUserDecryptionInstructionData;
};

export function parseRevokeDelegationForUserDecryptionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeDelegationForUserDecryptionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      delegator: getNextAccount(),
      hostConfig: getNextAccount(),
      delegationRecord: getNextAccount(),
    },
    data: getRevokeDelegationForUserDecryptionInstructionDataDecoder().decode(instruction.data),
  };
}
