/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findHostConfigPda } from '../pdas/index.js';
import { ZAMA_HOST_PROGRAM_ADDRESS } from '../programAddress.js';

export const VERIFY_PUBLIC_DECRYPT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  128, 196, 76, 98, 90, 3, 245, 119,
]);

export function getVerifyPublicDecryptDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(VERIFY_PUBLIC_DECRYPT_DISCRIMINATOR);
}

export type VerifyPublicDecryptInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountHostConfig extends string | AccountMeta<string> = string,
  TAccountKmsContext extends string | AccountMeta<string> = string,
  TAccountEncryptedValue extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountHostConfig extends string ? ReadonlyAccount<TAccountHostConfig> : TAccountHostConfig,
      TAccountKmsContext extends string ? ReadonlyAccount<TAccountKmsContext> : TAccountKmsContext,
      TAccountEncryptedValue extends string ? ReadonlyAccount<TAccountEncryptedValue> : TAccountEncryptedValue,
      ...TRemainingAccounts,
    ]
  >;

export type VerifyPublicDecryptInstructionData = {
  discriminator: ReadonlyUint8Array;
  handle: ReadonlyUint8Array;
  cleartext: ReadonlyUint8Array;
  signatures: Array<ReadonlyUint8Array>;
  extraData: ReadonlyUint8Array;
  /** Index of the proven leaf within the lineage's MMR. */
  leafIndex: bigint;
  /** Authentication path from the leaf up to its mountain peak. */
  siblings: Array<ReadonlyUint8Array>;
};

export type VerifyPublicDecryptInstructionDataArgs = {
  handle: ReadonlyUint8Array;
  cleartext: ReadonlyUint8Array;
  signatures: Array<ReadonlyUint8Array>;
  extraData: ReadonlyUint8Array;
  /** Index of the proven leaf within the lineage's MMR. */
  leafIndex: number | bigint;
  /** Authentication path from the leaf up to its mountain peak. */
  siblings: Array<ReadonlyUint8Array>;
};

export function getVerifyPublicDecryptInstructionDataEncoder(): Encoder<VerifyPublicDecryptInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['handle', fixEncoderSize(getBytesEncoder(), 32)],
      ['cleartext', fixEncoderSize(getBytesEncoder(), 32)],
      ['signatures', getArrayEncoder(fixEncoderSize(getBytesEncoder(), 65))],
      ['extraData', addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
      ['leafIndex', getU64Encoder()],
      ['siblings', getArrayEncoder(fixEncoderSize(getBytesEncoder(), 32))],
    ]),
    (value) => ({
      ...value,
      discriminator: VERIFY_PUBLIC_DECRYPT_DISCRIMINATOR,
    }),
  );
}

export function getVerifyPublicDecryptInstructionDataDecoder(): Decoder<VerifyPublicDecryptInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['handle', fixDecoderSize(getBytesDecoder(), 32)],
    ['cleartext', fixDecoderSize(getBytesDecoder(), 32)],
    ['signatures', getArrayDecoder(fixDecoderSize(getBytesDecoder(), 65))],
    ['extraData', addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    ['leafIndex', getU64Decoder()],
    ['siblings', getArrayDecoder(fixDecoderSize(getBytesDecoder(), 32))],
  ]);
}

export function getVerifyPublicDecryptInstructionDataCodec(): Codec<
  VerifyPublicDecryptInstructionDataArgs,
  VerifyPublicDecryptInstructionData
> {
  return combineCodec(getVerifyPublicDecryptInstructionDataEncoder(), getVerifyPublicDecryptInstructionDataDecoder());
}

export type VerifyPublicDecryptAsyncInput<
  TAccountHostConfig extends string = string,
  TAccountKmsContext extends string = string,
  TAccountEncryptedValue extends string = string,
> = {
  /**
   * Canonical singleton host config: source of the current KMS context id and the gateway
   * EIP-712 domain (chain id + `Decryption` verifying contract).
   */
  hostConfig?: Address<TAccountHostConfig>;
  /**
   * KMS context PDA; must be the canonical PDA for the id the certificate commits to in its
   * signed `extra_data`, and must not be destroyed. Verified in the handler.
   */
  kmsContext: Address<TAccountKmsContext>;
  /** The lineage whose peaks the inclusion proof is checked against. */
  encryptedValue: Address<TAccountEncryptedValue>;
  handle: VerifyPublicDecryptInstructionDataArgs['handle'];
  cleartext: VerifyPublicDecryptInstructionDataArgs['cleartext'];
  signatures: VerifyPublicDecryptInstructionDataArgs['signatures'];
  extraData: VerifyPublicDecryptInstructionDataArgs['extraData'];
  leafIndex: VerifyPublicDecryptInstructionDataArgs['leafIndex'];
  siblings: VerifyPublicDecryptInstructionDataArgs['siblings'];
};

export async function getVerifyPublicDecryptInstructionAsync<
  TAccountHostConfig extends string,
  TAccountKmsContext extends string,
  TAccountEncryptedValue extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: VerifyPublicDecryptAsyncInput<TAccountHostConfig, TAccountKmsContext, TAccountEncryptedValue>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  VerifyPublicDecryptInstruction<TProgramAddress, TAccountHostConfig, TAccountKmsContext, TAccountEncryptedValue>
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    kmsContext: { value: input.kmsContext ?? null, isWritable: false },
    encryptedValue: { value: input.encryptedValue ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hostConfig.value) {
    accounts.hostConfig.value = await findHostConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('kmsContext', accounts.kmsContext),
      getAccountMeta('encryptedValue', accounts.encryptedValue),
    ],
    data: getVerifyPublicDecryptInstructionDataEncoder().encode(args as VerifyPublicDecryptInstructionDataArgs),
    programAddress,
  } as VerifyPublicDecryptInstruction<TProgramAddress, TAccountHostConfig, TAccountKmsContext, TAccountEncryptedValue>);
}

export type VerifyPublicDecryptInput<
  TAccountHostConfig extends string = string,
  TAccountKmsContext extends string = string,
  TAccountEncryptedValue extends string = string,
> = {
  /**
   * Canonical singleton host config: source of the current KMS context id and the gateway
   * EIP-712 domain (chain id + `Decryption` verifying contract).
   */
  hostConfig: Address<TAccountHostConfig>;
  /**
   * KMS context PDA; must be the canonical PDA for the id the certificate commits to in its
   * signed `extra_data`, and must not be destroyed. Verified in the handler.
   */
  kmsContext: Address<TAccountKmsContext>;
  /** The lineage whose peaks the inclusion proof is checked against. */
  encryptedValue: Address<TAccountEncryptedValue>;
  handle: VerifyPublicDecryptInstructionDataArgs['handle'];
  cleartext: VerifyPublicDecryptInstructionDataArgs['cleartext'];
  signatures: VerifyPublicDecryptInstructionDataArgs['signatures'];
  extraData: VerifyPublicDecryptInstructionDataArgs['extraData'];
  leafIndex: VerifyPublicDecryptInstructionDataArgs['leafIndex'];
  siblings: VerifyPublicDecryptInstructionDataArgs['siblings'];
};

export function getVerifyPublicDecryptInstruction<
  TAccountHostConfig extends string,
  TAccountKmsContext extends string,
  TAccountEncryptedValue extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: VerifyPublicDecryptInput<TAccountHostConfig, TAccountKmsContext, TAccountEncryptedValue>,
  config?: { programAddress?: TProgramAddress },
): VerifyPublicDecryptInstruction<TProgramAddress, TAccountHostConfig, TAccountKmsContext, TAccountEncryptedValue> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    kmsContext: { value: input.kmsContext ?? null, isWritable: false },
    encryptedValue: { value: input.encryptedValue ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('kmsContext', accounts.kmsContext),
      getAccountMeta('encryptedValue', accounts.encryptedValue),
    ],
    data: getVerifyPublicDecryptInstructionDataEncoder().encode(args as VerifyPublicDecryptInstructionDataArgs),
    programAddress,
  } as VerifyPublicDecryptInstruction<TProgramAddress, TAccountHostConfig, TAccountKmsContext, TAccountEncryptedValue>);
}

export type ParsedVerifyPublicDecryptInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Canonical singleton host config: source of the current KMS context id and the gateway
     * EIP-712 domain (chain id + `Decryption` verifying contract).
     */
    hostConfig: TAccountMetas[0];
    /**
     * KMS context PDA; must be the canonical PDA for the id the certificate commits to in its
     * signed `extra_data`, and must not be destroyed. Verified in the handler.
     */
    kmsContext: TAccountMetas[1];
    /** The lineage whose peaks the inclusion proof is checked against. */
    encryptedValue: TAccountMetas[2];
  };
  data: VerifyPublicDecryptInstructionData;
};

export function parseVerifyPublicDecryptInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedVerifyPublicDecryptInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 3,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      hostConfig: getNextAccount(),
      kmsContext: getNextAccount(),
      encryptedValue: getNextAccount(),
    },
    data: getVerifyPublicDecryptInstructionDataDecoder().decode(instruction.data),
  };
}
