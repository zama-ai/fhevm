/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findHostConfigPda } from '../pdas/index.js';
import { ZAMA_HOST_PROGRAM_ADDRESS } from '../programAddress.js';

export const DELEGATE_FOR_USER_DECRYPTION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  240, 248, 215, 88, 109, 244, 1, 103,
]);

export function getDelegateForUserDecryptionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DELEGATE_FOR_USER_DECRYPTION_DISCRIMINATOR);
}

export type DelegateForUserDecryptionInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountDelegator extends string | AccountMeta<string> = string,
  TAccountHostConfig extends string | AccountMeta<string> = string,
  TAccountDelegationRecord extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountDelegator extends string
        ? ReadonlySignerAccount<TAccountDelegator> & AccountSignerMeta<TAccountDelegator>
        : TAccountDelegator,
      TAccountHostConfig extends string ? ReadonlyAccount<TAccountHostConfig> : TAccountHostConfig,
      TAccountDelegationRecord extends string ? WritableAccount<TAccountDelegationRecord> : TAccountDelegationRecord,
      TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DelegateForUserDecryptionInstructionData = {
  discriminator: ReadonlyUint8Array;
  delegate: Address;
  encryptedValueAccountAuthority: Address;
  expirationSlot: bigint;
};

export type DelegateForUserDecryptionInstructionDataArgs = {
  delegate: Address;
  encryptedValueAccountAuthority: Address;
  expirationSlot: number | bigint;
};

export function getDelegateForUserDecryptionInstructionDataEncoder(): FixedSizeEncoder<DelegateForUserDecryptionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['delegate', getAddressEncoder()],
      ['encryptedValueAccountAuthority', getAddressEncoder()],
      ['expirationSlot', getU64Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: DELEGATE_FOR_USER_DECRYPTION_DISCRIMINATOR,
    }),
  );
}

export function getDelegateForUserDecryptionInstructionDataDecoder(): FixedSizeDecoder<DelegateForUserDecryptionInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['delegate', getAddressDecoder()],
    ['encryptedValueAccountAuthority', getAddressDecoder()],
    ['expirationSlot', getU64Decoder()],
  ]);
}

export function getDelegateForUserDecryptionInstructionDataCodec(): FixedSizeCodec<
  DelegateForUserDecryptionInstructionDataArgs,
  DelegateForUserDecryptionInstructionData
> {
  return combineCodec(
    getDelegateForUserDecryptionInstructionDataEncoder(),
    getDelegateForUserDecryptionInstructionDataDecoder(),
  );
}

export type DelegateForUserDecryptionAsyncInput<
  TAccountPayer extends string = string,
  TAccountDelegator extends string = string,
  TAccountHostConfig extends string = string,
  TAccountDelegationRecord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays rent if the delegation PDA must be created. */
  payer: TransactionSigner<TAccountPayer>;
  /** User granting delegated decrypt rights. */
  delegator: TransactionSigner<TAccountDelegator>;
  /** Singleton config PDA. */
  hostConfig?: Address<TAccountHostConfig>;
  delegationRecord: Address<TAccountDelegationRecord>;
  /** System program used for account creation. */
  systemProgram?: Address<TAccountSystemProgram>;
  delegate: DelegateForUserDecryptionInstructionDataArgs['delegate'];
  encryptedValueAccountAuthority: DelegateForUserDecryptionInstructionDataArgs['encryptedValueAccountAuthority'];
  expirationSlot: DelegateForUserDecryptionInstructionDataArgs['expirationSlot'];
};

export async function getDelegateForUserDecryptionInstructionAsync<
  TAccountPayer extends string,
  TAccountDelegator extends string,
  TAccountHostConfig extends string,
  TAccountDelegationRecord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: DelegateForUserDecryptionAsyncInput<
    TAccountPayer,
    TAccountDelegator,
    TAccountHostConfig,
    TAccountDelegationRecord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DelegateForUserDecryptionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountDelegator,
    TAccountHostConfig,
    TAccountDelegationRecord,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    delegator: { value: input.delegator ?? null, isWritable: false },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    delegationRecord: {
      value: input.delegationRecord ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.hostConfig.value) {
    accounts.hostConfig.value = await findHostConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('delegator', accounts.delegator),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('delegationRecord', accounts.delegationRecord),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getDelegateForUserDecryptionInstructionDataEncoder().encode(
      args as DelegateForUserDecryptionInstructionDataArgs,
    ),
    programAddress,
  } as DelegateForUserDecryptionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountDelegator,
    TAccountHostConfig,
    TAccountDelegationRecord,
    TAccountSystemProgram
  >);
}

export type DelegateForUserDecryptionInput<
  TAccountPayer extends string = string,
  TAccountDelegator extends string = string,
  TAccountHostConfig extends string = string,
  TAccountDelegationRecord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays rent if the delegation PDA must be created. */
  payer: TransactionSigner<TAccountPayer>;
  /** User granting delegated decrypt rights. */
  delegator: TransactionSigner<TAccountDelegator>;
  /** Singleton config PDA. */
  hostConfig: Address<TAccountHostConfig>;
  delegationRecord: Address<TAccountDelegationRecord>;
  /** System program used for account creation. */
  systemProgram?: Address<TAccountSystemProgram>;
  delegate: DelegateForUserDecryptionInstructionDataArgs['delegate'];
  encryptedValueAccountAuthority: DelegateForUserDecryptionInstructionDataArgs['encryptedValueAccountAuthority'];
  expirationSlot: DelegateForUserDecryptionInstructionDataArgs['expirationSlot'];
};

export function getDelegateForUserDecryptionInstruction<
  TAccountPayer extends string,
  TAccountDelegator extends string,
  TAccountHostConfig extends string,
  TAccountDelegationRecord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ZAMA_HOST_PROGRAM_ADDRESS,
>(
  input: DelegateForUserDecryptionInput<
    TAccountPayer,
    TAccountDelegator,
    TAccountHostConfig,
    TAccountDelegationRecord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DelegateForUserDecryptionInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountDelegator,
  TAccountHostConfig,
  TAccountDelegationRecord,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? ZAMA_HOST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    delegator: { value: input.delegator ?? null, isWritable: false },
    hostConfig: { value: input.hostConfig ?? null, isWritable: false },
    delegationRecord: {
      value: input.delegationRecord ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta('payer', accounts.payer),
      getAccountMeta('delegator', accounts.delegator),
      getAccountMeta('hostConfig', accounts.hostConfig),
      getAccountMeta('delegationRecord', accounts.delegationRecord),
      getAccountMeta('systemProgram', accounts.systemProgram),
    ],
    data: getDelegateForUserDecryptionInstructionDataEncoder().encode(
      args as DelegateForUserDecryptionInstructionDataArgs,
    ),
    programAddress,
  } as DelegateForUserDecryptionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountDelegator,
    TAccountHostConfig,
    TAccountDelegationRecord,
    TAccountSystemProgram
  >);
}

export type ParsedDelegateForUserDecryptionInstruction<
  TProgram extends string = typeof ZAMA_HOST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays rent if the delegation PDA must be created. */
    payer: TAccountMetas[0];
    /** User granting delegated decrypt rights. */
    delegator: TAccountMetas[1];
    /** Singleton config PDA. */
    hostConfig: TAccountMetas[2];
    delegationRecord: TAccountMetas[3];
    /** System program used for account creation. */
    systemProgram: TAccountMetas[4];
  };
  data: DelegateForUserDecryptionInstructionData;
};

export function parseDelegateForUserDecryptionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedDelegateForUserDecryptionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 5,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      delegator: getNextAccount(),
      hostConfig: getNextAccount(),
      delegationRecord: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDelegateForUserDecryptionInstructionDataDecoder().decode(instruction.data),
  };
}
