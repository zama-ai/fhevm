/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Codec,
  type Decoder,
  type Encoder,
  type ReadonlyUint8Array,
} from '@solana/kit';

/**
 * Anchor-native mirror of `zama_solana_acl::MmrProof` for use as an instruction argument. The
 * shared ACL crate is deliberately Anchor-free (pure `borsh`) so it cannot derive Anchor IDL
 * metadata; this local type carries the identical wire shape and converts into the shared proof.
 * Defined in the host program so CPI callers depend only on the host IDL, never on token types.
 */
export type MmrInclusionProof = {
  /** Index of the proven leaf within the lineage's MMR. */
  leafIndex: bigint;
  /** Authentication path from the leaf up to its mountain peak. */
  siblings: Array<ReadonlyUint8Array>;
};

export type MmrInclusionProofArgs = {
  /** Index of the proven leaf within the lineage's MMR. */
  leafIndex: number | bigint;
  /** Authentication path from the leaf up to its mountain peak. */
  siblings: Array<ReadonlyUint8Array>;
};

export function getMmrInclusionProofEncoder(): Encoder<MmrInclusionProofArgs> {
  return getStructEncoder([
    ['leafIndex', getU64Encoder()],
    ['siblings', getArrayEncoder(fixEncoderSize(getBytesEncoder(), 32))],
  ]);
}

export function getMmrInclusionProofDecoder(): Decoder<MmrInclusionProof> {
  return getStructDecoder([
    ['leafIndex', getU64Decoder()],
    ['siblings', getArrayDecoder(fixDecoderSize(getBytesDecoder(), 32))],
  ]);
}

export function getMmrInclusionProofCodec(): Codec<MmrInclusionProofArgs, MmrInclusionProof> {
  return combineCodec(getMmrInclusionProofEncoder(), getMmrInclusionProofDecoder());
}
