/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';
import {
  getBatchStatusDecoder,
  getBatchStatusEncoder,
  type BatchStatus,
  type BatchStatusArgs,
} from '../types/index.js';

export const BATCH_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([156, 194, 70, 44, 22, 88, 137, 44]);

export function getBatchDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BATCH_DISCRIMINATOR);
}

export type Batch = {
  discriminator: ReadonlyUint8Array;
  /** Batcher config this batch belongs to (carries the direction). */
  batcher: Address;
  /** Zero-based index within the batcher. */
  index: bigint;
  /** Lifecycle status. */
  status: BatchStatus;
  /** Slot the batch opened at; dispatch requires `min_batch_age_slots` past. */
  openedSlot: bigint;
  /** Number of join calls routed into this batch. */
  joinCount: bigint;
  /** Bump of the per-batch authority PDA. */
  authorityBump: number;
  /** PDA bump for `(batcher, index)`. */
  bump: number;
  /** Born-public handle of the burned batch total (set at dispatch). */
  burnedTotalHandle: ReadonlyUint8Array;
  /** KMS-certified batch join total (set at settle; public by design). */
  totalJoined: bigint;
  /** Payout units the vault phase produced for the batch total (set at settle). */
  payoutReceived: bigint;
  /**
   * Informational rate `payout_received * RATE_SCALE / total_joined`,
   * saturating at u64::MAX (settle). Claims use exact proportional division
   * instead — never this field.
   */
  payoutRate: bigint;
};

export type BatchArgs = {
  /** Batcher config this batch belongs to (carries the direction). */
  batcher: Address;
  /** Zero-based index within the batcher. */
  index: number | bigint;
  /** Lifecycle status. */
  status: BatchStatusArgs;
  /** Slot the batch opened at; dispatch requires `min_batch_age_slots` past. */
  openedSlot: number | bigint;
  /** Number of join calls routed into this batch. */
  joinCount: number | bigint;
  /** Bump of the per-batch authority PDA. */
  authorityBump: number;
  /** PDA bump for `(batcher, index)`. */
  bump: number;
  /** Born-public handle of the burned batch total (set at dispatch). */
  burnedTotalHandle: ReadonlyUint8Array;
  /** KMS-certified batch join total (set at settle; public by design). */
  totalJoined: number | bigint;
  /** Payout units the vault phase produced for the batch total (set at settle). */
  payoutReceived: number | bigint;
  /**
   * Informational rate `payout_received * RATE_SCALE / total_joined`,
   * saturating at u64::MAX (settle). Claims use exact proportional division
   * instead — never this field.
   */
  payoutRate: number | bigint;
};

/** Gets the encoder for {@link BatchArgs} account data. */
export function getBatchEncoder(): FixedSizeEncoder<BatchArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['batcher', getAddressEncoder()],
      ['index', getU64Encoder()],
      ['status', getBatchStatusEncoder()],
      ['openedSlot', getU64Encoder()],
      ['joinCount', getU64Encoder()],
      ['authorityBump', getU8Encoder()],
      ['bump', getU8Encoder()],
      ['burnedTotalHandle', fixEncoderSize(getBytesEncoder(), 32)],
      ['totalJoined', getU64Encoder()],
      ['payoutReceived', getU64Encoder()],
      ['payoutRate', getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BATCH_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Batch} account data. */
export function getBatchDecoder(): FixedSizeDecoder<Batch> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['batcher', getAddressDecoder()],
    ['index', getU64Decoder()],
    ['status', getBatchStatusDecoder()],
    ['openedSlot', getU64Decoder()],
    ['joinCount', getU64Decoder()],
    ['authorityBump', getU8Decoder()],
    ['bump', getU8Decoder()],
    ['burnedTotalHandle', fixDecoderSize(getBytesDecoder(), 32)],
    ['totalJoined', getU64Decoder()],
    ['payoutReceived', getU64Decoder()],
    ['payoutRate', getU64Decoder()],
  ]);
}

/** Gets the codec for {@link Batch} account data. */
export function getBatchCodec(): FixedSizeCodec<BatchArgs, Batch> {
  return combineCodec(getBatchEncoder(), getBatchDecoder());
}

export function decodeBatch<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Batch, TAddress>;
export function decodeBatch<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Batch, TAddress>;
export function decodeBatch<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Batch, TAddress> | MaybeAccount<Batch, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getBatchDecoder());
}

export async function fetchBatch<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Batch, TAddress>> {
  const maybeAccount = await fetchMaybeBatch(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBatch<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Batch, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBatch(maybeAccount);
}

export async function fetchAllBatch(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Batch>[]> {
  const maybeAccounts = await fetchAllMaybeBatch(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBatch(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Batch>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeBatch(maybeAccount));
}

export function getBatchSize(): number {
  return 123;
}
