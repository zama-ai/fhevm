/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from '@solana/kit';

export const JOIN_RECORD_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([179, 126, 223, 146, 242, 12, 148, 5]);

export function getJoinRecordDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(JOIN_RECORD_DISCRIMINATOR);
}

export type JoinRecord = {
  discriminator: ReadonlyUint8Array;
  /** Batch this record belongs to. */
  batch: Address;
  /** Joining user. */
  user: Address;
  /** `EncryptedValue` lineage holding the user's accumulated joined amount. */
  joinedEncryptedValue: Address;
  /** Whether the user's payout was claimed. */
  claimed: boolean;
  /** PDA bump for `(batch, user)`. */
  bump: number;
};

export type JoinRecordArgs = {
  /** Batch this record belongs to. */
  batch: Address;
  /** Joining user. */
  user: Address;
  /** `EncryptedValue` lineage holding the user's accumulated joined amount. */
  joinedEncryptedValue: Address;
  /** Whether the user's payout was claimed. */
  claimed: boolean;
  /** PDA bump for `(batch, user)`. */
  bump: number;
};

/** Gets the encoder for {@link JoinRecordArgs} account data. */
export function getJoinRecordEncoder(): FixedSizeEncoder<JoinRecordArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['batch', getAddressEncoder()],
      ['user', getAddressEncoder()],
      ['joinedEncryptedValue', getAddressEncoder()],
      ['claimed', getBooleanEncoder()],
      ['bump', getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: JOIN_RECORD_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link JoinRecord} account data. */
export function getJoinRecordDecoder(): FixedSizeDecoder<JoinRecord> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['batch', getAddressDecoder()],
    ['user', getAddressDecoder()],
    ['joinedEncryptedValue', getAddressDecoder()],
    ['claimed', getBooleanDecoder()],
    ['bump', getU8Decoder()],
  ]);
}

/** Gets the codec for {@link JoinRecord} account data. */
export function getJoinRecordCodec(): FixedSizeCodec<JoinRecordArgs, JoinRecord> {
  return combineCodec(getJoinRecordEncoder(), getJoinRecordDecoder());
}

export function decodeJoinRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<JoinRecord, TAddress>;
export function decodeJoinRecord<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<JoinRecord, TAddress>;
export function decodeJoinRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<JoinRecord, TAddress> | MaybeAccount<JoinRecord, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getJoinRecordDecoder());
}

export async function fetchJoinRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<JoinRecord, TAddress>> {
  const maybeAccount = await fetchMaybeJoinRecord(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeJoinRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<JoinRecord, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeJoinRecord(maybeAccount);
}

export async function fetchAllJoinRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<JoinRecord>[]> {
  const maybeAccounts = await fetchAllMaybeJoinRecord(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeJoinRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<JoinRecord>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeJoinRecord(maybeAccount));
}

export function getJoinRecordSize(): number {
  return 106;
}
