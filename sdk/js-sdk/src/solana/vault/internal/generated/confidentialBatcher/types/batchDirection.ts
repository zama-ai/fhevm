/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getEnumDecoder,
  getEnumEncoder,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from '@solana/kit';

/**
 * Which way through the vault a batcher instance moves value. Direction lives
 * on the `Batcher` config — one instance per direction, mirroring the EVM's
 * two batcher deployments — so a pending deposit batch never blocks a redeem
 * batch, and every batch inherits its batcher's direction.
 */
export enum BatchDirection {
  Deposit,
  Redeem,
}

export type BatchDirectionArgs = BatchDirection;

export function getBatchDirectionEncoder(): FixedSizeEncoder<BatchDirectionArgs> {
  return getEnumEncoder(BatchDirection);
}

export function getBatchDirectionDecoder(): FixedSizeDecoder<BatchDirection> {
  return getEnumDecoder(BatchDirection);
}

export function getBatchDirectionCodec(): FixedSizeCodec<BatchDirectionArgs, BatchDirection> {
  return combineCodec(getBatchDirectionEncoder(), getBatchDirectionDecoder());
}
