export const metadata = {
  title: 'fhevm wasmBaseUrl Next.js smoke test',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
