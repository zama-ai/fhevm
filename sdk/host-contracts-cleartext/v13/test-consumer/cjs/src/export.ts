// AUTOGENERATED by `fhevm-npm generate-exports` from ../../../export.manifest.json.
// DO NOT EDIT — your changes will be overwritten.

import type {
  AbstractEthereumProvider,
  AbstractEthereumUtils,
  AbstractEthereumSigner,
  DeployParameters,
  DeployReturnType,
  EncodeCallParameters,
  FhevmAddresses,
  CleartextAddresses,
  FhevmAddressesV12,
  BootstrapConfig,
  Deployed,
  ProtocolConfigInitConfig,
  InputVerifierInitConfig,
  KMSVerifierInitConfig,
  HCULimitInitConfig,
  KmsNode,
  KmsThresholds,
  UpdateV12ToV13MigrationConfig,
  AbstractEthereumHistory,
  PartialStack,
  SnapshotParameters,
  StackSnapshot,
  VerifyCheck,
  VerifyExpectations,
  VerifyParameters,
  VerifyReport,
  Create2Parameters,
  Create2Addresses,
} from '@fhevm/host-contracts-cleartext/ts' with { 'resolution-mode': 'require' };

import assert from 'node:assert/strict';
import test from 'node:test';

const {
  deploy,
  updateV12ToV13,
  precomputeAddresses,
  precomputeCreate2Addresses,
  CREATE2_FACTORY,
  CREATE2_ROLES,
  setupACLOwner,
  pauseACL,
  unpauseACL,
  defineNewKmsContext,
  destroyKmsContext,
  CONTRACT_VERSIONS,
  verify,
  snapshotStack,
  DEFAULT_MAY_CHANGE,
  CLEARTEXT_GATEWAY_CHAIN_ID,
  CLEARTEXT_DECRYPTION_ADDRESS,
  CLEARTEXT_INPUT_VERIFICATION_ADDRESS,
} = require('@fhevm/host-contracts-cleartext/ts') as typeof import('@fhevm/host-contracts-cleartext/ts', {
  with: { 'resolution-mode': 'require' },
});

const ADDRESS: string = '0x0000000000000000000000000000000000000000';
const HEX: `0x${string}` = '0x';

////////////////////////////////////////////////////////////////////////////////
// One sample value per exported type. These are compile-time assertions: each binding resolves only if
// its type is still exported and still describes the value, so a renamed field fails the fixture's tsc.
////////////////////////////////////////////////////////////////////////////////

const dummyEncodeCallParameters: EncodeCallParameters = { abi: [], functionName: 'owner', args: [] };
const dummyDeployParameters: DeployParameters = { abi: [], bytecode: HEX, args: [] };
const dummyDeployReturnType: DeployReturnType = { contractAddress: ADDRESS };
const dummyAbstractEthereumUtils: AbstractEthereumUtils = {
  getContractAddress: () => HEX,
  encodeCall: () => Promise.resolve(HEX),
  keccak256: () => HEX,
  encodeAbiParameters: () => HEX,
  getCreate2Address: () => HEX,
};
const dummyAbstractEthereumProvider: AbstractEthereumProvider = {
  setCodeAt: () => Promise.resolve(),
  getCodeAt: () => Promise.resolve(HEX),
  readContract: () => Promise.resolve(undefined),
  getTransactionCount: () => Promise.resolve(0),
};
const dummyAbstractEthereumHistory: AbstractEthereumHistory = {
  getBlockNumber: () => Promise.resolve(0n),
  getStorageAt: () => Promise.resolve(HEX),
  getLogs: () => Promise.resolve([]),
};
const dummyAbstractEthereumSigner: AbstractEthereumSigner = {
  getAddress: () => Promise.resolve(ADDRESS),
  deploy: () => Promise.resolve({ contractAddress: ADDRESS }),
  writeContract: () => Promise.resolve(undefined),
};
const dummyFhevmAddresses: FhevmAddresses = {
  aclAddress: ADDRESS,
  fhevmExecutorAddress: ADDRESS,
  kmsVerifierAddress: ADDRESS,
  inputVerifierAddress: ADDRESS,
  hcuLimitAddress: ADDRESS,
  protocolConfigAddress: ADDRESS,
  kmsGenerationAddress: ADDRESS,
};
const dummyFhevmAddressesV12: FhevmAddressesV12 = {
  aclAddress: ADDRESS,
  fhevmExecutorAddress: ADDRESS,
  kmsVerifierAddress: ADDRESS,
  inputVerifierAddress: ADDRESS,
  hcuLimitAddress: ADDRESS,
};
const dummyCleartextAddresses: CleartextAddresses = {
  cleartextArithmeticAddress: ADDRESS,
  cleartextDbAddress: ADDRESS,
};
const dummyKmsNode: KmsNode = {
  txSenderAddress: ADDRESS,
  signerAddress: ADDRESS,
  ipAddress: '127.0.0.1',
  storageUrl: 's3://kms-bucket-0',
};
const dummyKmsThresholds: KmsThresholds = { publicDecryption: 1n, userDecryption: 1n, kmsGen: 1n, mpc: 1n };
const dummyInputVerifierInitConfig: InputVerifierInitConfig = {
  verifyingContractSource: ADDRESS,
  chainIDSource: 1n,
  initialSigners: [ADDRESS],
  initialThreshold: 1n,
};
const dummyKMSVerifierInitConfig: KMSVerifierInitConfig = { verifyingContractSource: ADDRESS, chainIDSource: 1n };
const dummyProtocolConfigInitConfig: ProtocolConfigInitConfig = {
  initialKmsNodes: [dummyKmsNode],
  initialThresholds: dummyKmsThresholds,
};
const dummyHCULimitInitConfig: HCULimitInitConfig = { hcuCapPerBlock: 1n, maxHCUDepthPerTx: 1n, maxHCUPerTx: 1n };
const dummyBootstrapConfig: BootstrapConfig = {
  kmsVerifier: dummyKMSVerifierInitConfig,
  inputVerifier: dummyInputVerifierInitConfig,
  hcuLimit: dummyHCULimitInitConfig,
  protocolConfig: dummyProtocolConfigInitConfig,
};
const dummyDeployed: Deployed = {
  fhevmAddresses: dummyFhevmAddresses,
  cleartextAddresses: dummyCleartextAddresses,
  pauserSetAddress: ADDRESS,
  aclOwnerAddress: ADDRESS,
};
const dummyUpdateV12ToV13MigrationConfig: UpdateV12ToV13MigrationConfig = {
  existingContextId: 1n,
  existingKmsNodes: [dummyKmsNode],
  existingThresholds: dummyKmsThresholds,
};
const dummyVerifyCheck: VerifyCheck = { name: 'ACL.getVersion', status: 'skip', detail: 'no history adapter' };
const dummyVerifyReport: VerifyReport = { ok: true, checks: [dummyVerifyCheck], failures: [], skipped: [] };
const dummyStackSnapshot: StackSnapshot = { blockNumber: 1n, readings: { 'ACL.getVersion': 'ACL v0.4.0' } };
const dummyVerifyExpectations: VerifyExpectations = {
  admin: ADDRESS,
  pausers: [ADDRESS],
  coprocessorSigners: [ADDRESS],
  coprocessorThreshold: 1n,
  kmsSigners: [ADDRESS],
  kmsThresholds: dummyKmsThresholds,
  kmsContextId: 1n,
};
const dummyPartialStack: PartialStack = { fhevmAddresses: { aclAddress: ADDRESS } };
const dummySnapshotParameters: SnapshotParameters = {
  ethProvider: dummyAbstractEthereumProvider,
  history: dummyAbstractEthereumHistory,
  deployed: dummyPartialStack,
  abis: {},
};
const dummyVerifyParameters: VerifyParameters = {
  mode: 'upgrade',
  before: dummyStackSnapshot,
  mayChange: DEFAULT_MAY_CHANGE,
  ethProvider: dummyAbstractEthereumProvider,
  history: dummyAbstractEthereumHistory,
  deployed: dummyDeployed,
  expected: dummyVerifyExpectations,
  abis: {},
};
const dummyCreate2Parameters: Create2Parameters = {
  ethUtils: dummyAbstractEthereumUtils,
  version: '0.13',
  deploymentId: 'export-test',
  deployer: ADDRESS,
  factory: CREATE2_FACTORY,
};
const dummyCreate2Addresses: Create2Addresses = {
  fhevmAddresses: dummyFhevmAddresses,
  cleartextAddresses: dummyCleartextAddresses,
  pauserSetAddress: ADDRESS,
  aclOwnerAddress: ADDRESS,
  emptyImplementations: { acl: ADDRESS, shared: ADDRESS },
  factory: CREATE2_FACTORY,
};

const SAMPLES: readonly unknown[] = [
  dummyEncodeCallParameters,
  dummyDeployParameters,
  dummyDeployReturnType,
  dummyAbstractEthereumUtils,
  dummyAbstractEthereumProvider,
  dummyAbstractEthereumHistory,
  dummyAbstractEthereumSigner,
  dummyFhevmAddresses,
  dummyFhevmAddressesV12,
  dummyCleartextAddresses,
  dummyKmsNode,
  dummyKmsThresholds,
  dummyInputVerifierInitConfig,
  dummyKMSVerifierInitConfig,
  dummyProtocolConfigInitConfig,
  dummyHCULimitInitConfig,
  dummyBootstrapConfig,
  dummyDeployed,
  dummyUpdateV12ToV13MigrationConfig,
  dummyVerifyCheck,
  dummyVerifyReport,
  dummyStackSnapshot,
  dummyVerifyExpectations,
  dummyPartialStack,
  dummySnapshotParameters,
  dummyVerifyParameters,
  dummyCreate2Parameters,
  dummyCreate2Addresses,
];

////////////////////////////////////////////////////////////////////////////////

void test('every value export arrives with the type a consumer relies on', () => {
  assert.equal(typeof deploy, 'function');
  assert.equal(typeof updateV12ToV13, 'function');
  assert.equal(typeof precomputeAddresses, 'function');
  assert.equal(typeof precomputeCreate2Addresses, 'function');
  assert.equal(typeof CREATE2_FACTORY, 'string');
  assert.equal(typeof CREATE2_ROLES, 'object');
  assert.equal(typeof CREATE2_ROLES.acl, 'string');
  assert.equal(typeof setupACLOwner, 'function');
  assert.equal(typeof pauseACL, 'function');
  assert.equal(typeof unpauseACL, 'function');
  assert.equal(typeof defineNewKmsContext, 'function');
  assert.equal(typeof destroyKmsContext, 'function');
  assert.equal(typeof CONTRACT_VERSIONS, 'object');
  assert.equal(typeof CONTRACT_VERSIONS.acl, 'string');
  assert.equal(typeof verify, 'function');
  assert.equal(typeof snapshotStack, 'function');
  assert.ok(Array.isArray(DEFAULT_MAY_CHANGE), 'DEFAULT_MAY_CHANGE must be an array');
  assert.equal(typeof DEFAULT_MAY_CHANGE[0], 'string');
  assert.equal(typeof CLEARTEXT_GATEWAY_CHAIN_ID, 'bigint');
  assert.equal(typeof CLEARTEXT_DECRYPTION_ADDRESS, 'string');
  assert.equal(typeof CLEARTEXT_INPUT_VERIFICATION_ADDRESS, 'string');
});

void test('every type export still describes its sample value', () => {
  // The assertion already happened, at compile time. This keeps the bindings from being dead code.
  for (const sample of SAMPLES) {
    assert.notEqual(sample, undefined);
  }
});
