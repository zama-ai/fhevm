/**
 * @deprecated Pool requirements are execution-agnostic and live in the pool
 * layer. This compatibility export keeps existing suite integrations stable.
 */
export * from "../pool/requirements";
