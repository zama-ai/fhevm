# Run artifacts: reports, raw JSONL time series, suite summaries.
# Regenerated by every run; never source-controlled. Baselines are committed
# separately under baselines/ as the regression reference.
runs/

pools/
