node_modules
.env

# Hardhat files
/cache
/artifacts
/types

# TypeChain files
/typechain
/typechain-types

# solidity-coverage files
/coverage
/coverage.json

# Proxy contract addresses
/addresses

# Hardhat Ignition default folder for deployments against a local node
ignition/deployments/chain-31337

*DS_Store

# Forge
/cache_forge
/out
lcov.info
/report
/lib/forge-std
foundry.lock


# Soldeer
/dependencies

# OZ artifacts
/.openzeppelin

# LayerZero wiring subproject build artifacts (the project itself is committed,
# but its node_modules + hardhat outputs are not — it is a self-contained
# pnpm workspace, see lz-wiring/README.md).
lz-wiring/node_modules
lz-wiring/cache
lz-wiring/artifacts
lz-wiring/dist

# Mock coprocessor persistent state (committed source, ignored runtime DB).
scripts/mock-coprocessor/*.db
scripts/mock-coprocessor/*.db-journal

addresses-amoy
addresses-sepolia
enc.out
