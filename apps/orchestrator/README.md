# Description

The orchestrator runs workflows and keep all other services in sync

# requirements

You need the [aws-cli](https://aws.amazon.com/cli/) to be installed

```
# install it
npm --filter app/orchestrator install

# run it
npm --filter app/orchestrator start

# play with it (generate test events)
make
```
