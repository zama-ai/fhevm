services:
  tests-e2e-debug:
    container_name: console-tests-e2e-debug
    image: ghcr.io/zama-ai/fhevm/test-suite/e2e:${TESTS_VERSION:-77dc4ec}
    env_file:
      - ./external/fhevm/test-suite/fhevm/env/staging/.env.test-suite.local
    environment:
      - RELAYER_URL=${RELAYER_URL:-http://console-back:3005}
    command:
      - tail -f /dev/null
    networks:
      - fhevm_default
      - console-net


networks:
  console-net:
    name: console-net
    external: true
  fhevm_default:
    name: fhevm_default
    external: true

