[workspace]
members = [
    "core",
    "program",
    "local-cli",
    "test-input-core",
    "test-input-program",
    "confidential-token-core",
    "confidential-token-program",
]
resolver = "2"

[profile.release]
overflow-checks = true
