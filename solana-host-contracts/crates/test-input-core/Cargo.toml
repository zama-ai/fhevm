[package]
name = "solana-test-input-core"
version = "0.1.0"
edition = "2021"
license = "BSD-3-Clause-Clear"
description = "Shared interface and state for the Solana TestInput app program"

[dependencies]
borsh = { version = "1.6.0", features = ["derive"] }
sha3 = "0.10.8"
solana-host-contracts-core = { path = "../core" }
solana-program = "4.0.0"
