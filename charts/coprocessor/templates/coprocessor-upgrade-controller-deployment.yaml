{{- if .Values.upgradeController.enabled -}}
{{- $disabled := default (list) .Values.upgradeController.disabledArgs -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: coprocessor-upgrade-controller
    app.kubernetes.io/name: {{ include "upgradeControllerName" . }}
  name: {{ include "upgradeControllerName" . }}
spec:
  replicas: {{ .Values.upgradeController.replicas }}
  selector:
    matchLabels:
      app: coprocessor-upgrade-controller
      {{- include "coprocessor.extraSelectorLabels" . | nindent 6 }}
  {{- if .Values.upgradeController.updateStrategy }}
  strategy:
    {{- toYaml .Values.upgradeController.updateStrategy | nindent 4 }}
  {{- end }}
  template:
    metadata:
      labels:
        app: coprocessor-upgrade-controller
        app.kubernetes.io/name: {{ include "upgradeControllerName" . }}
        {{- include "coprocessor.extraSelectorLabels" . | nindent 8 }}
        {{- with .Values.commonConfig.stackVersion }}
        app.kubernetes.io/version: {{ . | quote }}
        {{- end }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.upgradeController.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      imagePullSecrets:
        - name: registry-credentials
      restartPolicy: Always
      {{- with .Values.upgradeController.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.upgradeController.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.upgradeController.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.upgradeController.serviceAccountName }}
      serviceAccountName: {{ .Values.upgradeController.serviceAccountName }}
      {{- end }}
      containers:
        - name: coprocessor-upgrade-controller
          image: {{ .Values.upgradeController.image.name }}:{{ .Values.upgradeController.image.tag }}
          command: ["upgrade-controller"]
          {{- if .Values.commonConfig.stackVersionProbe.enabled }}
          startupProbe:
            exec:
              command: ["upgrade-controller", "--stack-version"]
            periodSeconds: {{ .Values.commonConfig.stackVersionProbe.periodSeconds | default 10 }}
            failureThreshold: {{ .Values.commonConfig.stackVersionProbe.failureThreshold | default 30 }}
          {{- end }}
          args:
            {{- if not (has "database-url" $disabled) }}
            - --database-url=$(DATABASE_URL)
            {{- end }}
            {{- with .Values.upgradeController.extraArgs }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          env:
            {{- with .Values.commonConfig.databaseUser }}
            - name: DATABASE_USER
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- with .Values.commonConfig.databaseEndpoint }}
            - name: DATABASE_ENDPOINT
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- if eq (include "coprocessorDatabaseAuthMode" .) "password" }}
            {{- with .Values.commonConfig.databasePassword }}
            - name: DATABASE_PASSWORD
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- end }}
            {{- with .Values.commonConfig.databaseUrl }}
            - name: DATABASE_URL
              value: {{ . | quote }}
            {{- end }}
            {{- if eq (include "coprocessorDatabaseAuthMode" .) "iam" }}
            - name: DATABASE_IAM_AUTH_ENABLED
              value: "true"
            {{- with .Values.commonConfig.databaseIamRegion }}
            - name: DATABASE_IAM_REGION
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- if eq (include "coprocessor.databaseSslRootCertEnvEnabled" .) "true" }}
            {{- include "coprocessor.databaseSslRootCertEnv" . | nindent 12 }}
            {{- end }}
            {{- end }}
          {{- if default .Values.commonConfig.tracing.enabled .Values.upgradeController.tracing.enabled }}
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ .Values.commonConfig.tracing.endpoint | quote }}
            - name: OTEL_SERVICE_NAME
              value: {{ .Values.upgradeController.tracing.service | quote }}
          {{- end }}
          {{- with .Values.commonConfig.env }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.upgradeController.env }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
          resources:
            requests:
              cpu: {{ .Values.upgradeController.resources.requests.cpu | default "100m" }}
              memory: {{ .Values.upgradeController.resources.requests.memory | default "256Mi" }}
            limits:
              cpu: {{ .Values.upgradeController.resources.limits.cpu | default "500m" }}
              memory: {{ .Values.upgradeController.resources.limits.memory | default "512Mi" }}
          {{- if and .Values.upgradeController.probes .Values.upgradeController.probes.liveness.enabled }}
          livenessProbe:
            {{- toYaml (omit .Values.upgradeController.probes.liveness "enabled") | nindent 12 }}
          {{- end }}
          {{- if and .Values.upgradeController.probes .Values.upgradeController.probes.readiness.enabled }}
          readinessProbe:
            {{- toYaml (omit .Values.upgradeController.probes.readiness "enabled") | nindent 12 }}
          {{- end }}
          {{- if eq (include "coprocessor.databaseSslRootCertMountEnabled" .) "true" }}
          {{- include "coprocessor.databaseSslRootCertVolumeMount" . | nindent 10 }}
          {{- end }}
      {{- if eq (include "coprocessor.databaseSslRootCertMountEnabled" .) "true" }}
      {{- include "coprocessor.databaseSslRootCertVolume" . | nindent 6 }}
      {{- end }}
{{- end -}}
