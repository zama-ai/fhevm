{{- range $i, $chain := .Values.chains }}
{{- if (($chain.hostListenerPoller).enabled) }}
{{- $useLegacyName := $chain.hostListenerPoller.useLegacyName | default false -}}
{{- $deploymentName := ternary (printf "%s-host-listener-poller" $.Release.Name) (printf "%s-host-listener-poller-%s" $.Release.Name $chain.name) $useLegacyName -}}
{{- $appLabel := ternary "coprocessor-host-listener-poller" (printf "coprocessor-host-listener-poller-%s" $chain.name) $useLegacyName -}}
{{- $args := default (dict) $.Values.hostListenerPollerShared.args -}}
{{- $disabled := default (list) $.Values.hostListenerPollerShared.disabledArgs -}}
{{- $serviceName := coalesce $chain.hostListenerPoller.serviceName (get $args "serviceName") -}}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: {{ $appLabel }}
    app.kubernetes.io/name: {{ $deploymentName }}
  name: {{ $deploymentName }}
spec:
  replicas: {{ $.Values.hostListenerPollerShared.replicas }}
  selector:
    matchLabels:
      app: {{ $appLabel }}
  {{- if $.Values.hostListenerPollerShared.updateStrategy }}
  strategy:
    {{- toYaml $.Values.hostListenerPollerShared.updateStrategy | nindent 4 }}
  {{- end }}
  template:
    metadata:
      labels:
        app: {{ $appLabel }}
        app.kubernetes.io/name: {{ $deploymentName }}
        {{- with $.Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with $.Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      imagePullSecrets:
        - name: registry-credentials
      restartPolicy: Always
      {{- with $.Values.hostListenerPollerShared.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $.Values.hostListenerPollerShared.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $.Values.hostListenerPollerShared.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if $.Values.hostListenerPollerShared.serviceAccountName }}
      serviceAccountName: {{ $.Values.hostListenerPollerShared.serviceAccountName }}
      {{- end }}
      containers:
        - name: coprocessor-host-listener-poller
          image: {{ $.Values.hostListenerPollerShared.image.name }}:{{ $.Values.hostListenerPollerShared.image.tag }}
          command: ["host_listener_poller"]
          args:
            {{- if not (has "database-url" $disabled) }}
            - --database-url=$(DATABASE_URL)
            {{- end }}
            {{- if not (has "url" $disabled) }}
            - --url=$(ETHEREUM_RPC_HTTP_URL)
            {{- end }}
            {{- if not (has "acl-contract-address" $disabled) }}
            - --acl-contract-address=$(ACL_CONTRACT_ADDRESS)
            {{- end }}
            {{- if not (has "tfhe-contract-address" $disabled) }}
            - --tfhe-contract-address=$(FHEVM_EXECUTOR_CONTRACT_ADDRESS)
            {{- end }}
            {{- if not (has "health-port" $disabled) }}
            - --health-port={{ $.Values.hostListenerPollerShared.ports.healthcheck }}
            {{- end }}
            {{- if not (has "metrics-addr" $disabled) }}
            - --metrics-addr=0.0.0.0:{{ $.Values.hostListenerPollerShared.ports.metrics }}
            {{- end }}
            {{- if and $serviceName (not (has "service-name" $disabled)) }}
            - --service-name={{ $serviceName }}
            {{- end }}
            {{- with $.Values.commonConfig.tenantApiKey }}
            {{- if and (or .value .valueFrom) (not (has "coprocessor-api-key" $disabled)) }}
            - --coprocessor-api-key=$(TENANT_API_KEY)
            {{- end }}
            {{- end }}
            {{- with $.Values.hostListenerPollerShared.extraArgs }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- with $chain.hostListenerPoller.extraArgs }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          env:
            {{- with $.Values.commonConfig.databaseUser }}
            - name: DATABASE_USER
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- with $.Values.commonConfig.databaseEndpoint }}
            - name: DATABASE_ENDPOINT
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- if eq (include "coprocessorDatabaseAuthMode" $) "password" }}
            {{- with $.Values.commonConfig.databasePassword }}
            - name: DATABASE_PASSWORD
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- end }}
            {{- with $.Values.commonConfig.databaseUrl }}
            - name: DATABASE_URL
              value: {{ . | quote }}
            {{- end }}
            {{- if eq (include "coprocessorDatabaseAuthMode" $) "iam" }}
            - name: DATABASE_IAM_AUTH_ENABLED
              value: "true"
            {{- with $.Values.commonConfig.databaseIamRegion }}
            - name: DATABASE_IAM_REGION
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- with $.Values.commonConfig.databaseSslRootCertPath }}
            - name: DATABASE_SSL_ROOT_CERT_PATH
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- end }}
            {{- with $.Values.commonConfig.tenantApiKey }}
            - name: TENANT_API_KEY
              {{- if .value }}
              value: {{ .value | quote }}
              {{- else if .valueFrom }}
              valueFrom:
                {{- toYaml .valueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- if or $chain.httpUrl $chain.httpUrlValueFrom }}
            - name: ETHEREUM_RPC_HTTP_URL
              {{- if $chain.httpUrl }}
              value: {{ $chain.httpUrl | quote }}
              {{- else if $chain.httpUrlValueFrom }}
              valueFrom:
                {{- toYaml $chain.httpUrlValueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- if or $chain.aclContractAddress $chain.aclContractAddressValueFrom }}
            - name: ACL_CONTRACT_ADDRESS
              {{- if $chain.aclContractAddress }}
              value: {{ $chain.aclContractAddress | quote }}
              {{- else if $chain.aclContractAddressValueFrom }}
              valueFrom:
                {{- toYaml $chain.aclContractAddressValueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
            {{- if or $chain.fhevmExecutorContractAddress $chain.fhevmExecutorContractAddressValueFrom }}
            - name: FHEVM_EXECUTOR_CONTRACT_ADDRESS
              {{- if $chain.fhevmExecutorContractAddress }}
              value: {{ $chain.fhevmExecutorContractAddress | quote }}
              {{- else if $chain.fhevmExecutorContractAddressValueFrom }}
              valueFrom:
                {{- toYaml $chain.fhevmExecutorContractAddressValueFrom | nindent 16 }}
              {{- end }}
            {{- end }}
          {{- if default $.Values.commonConfig.tracing.enabled $.Values.hostListenerPollerShared.tracing.enabled }}
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ $.Values.commonConfig.tracing.endpoint | quote }}
            - name: OTEL_SERVICE_NAME
              {{- if $.Values.hostListenerPollerShared.tracing.service }}
              value: {{ $.Values.hostListenerPollerShared.tracing.service | quote }}
              {{- else }}
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
              {{- end }}
          {{- end }}
          {{- with $.Values.commonConfig.env }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with $.Values.hostListenerPollerShared.env }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with $chain.hostListenerPoller.env }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            {{- range $portName, $portValue := $.Values.hostListenerPollerShared.ports }}
            - name: {{ $portName }}
              containerPort: {{ $portValue }}
              protocol: TCP
            {{- end }}
          resources:
            requests:
              cpu: {{ $.Values.hostListenerPollerShared.resources.requests.cpu | default "100m" }}
              memory: {{ $.Values.hostListenerPollerShared.resources.requests.memory | default "256Mi" }}
            limits:
              cpu: {{ $.Values.hostListenerPollerShared.resources.limits.cpu | default "500m" }}
              memory: {{ $.Values.hostListenerPollerShared.resources.limits.memory | default "512Mi" }}
          {{- if and $.Values.hostListenerPollerShared.probes $.Values.hostListenerPollerShared.probes.liveness.enabled }}
          livenessProbe:
            {{- toYaml (omit $.Values.hostListenerPollerShared.probes.liveness "enabled") | nindent 12 }}
          {{- end }}
          {{- if and $.Values.hostListenerPollerShared.probes $.Values.hostListenerPollerShared.probes.readiness.enabled }}
          readinessProbe:
            {{- toYaml (omit $.Values.hostListenerPollerShared.probes.readiness "enabled") | nindent 12 }}
          {{- end }}
{{- end }}
{{- end }}
