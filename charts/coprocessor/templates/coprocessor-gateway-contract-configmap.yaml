{{- if .Values.gatewayContractConfigMap.enabled -}}
{{- $cm := .Values.gatewayContractConfigMap -}}
{{- $allowed := list "" "devnet" "testnet" "mainnet" -}}
{{- $network := default "" $cm.network -}}
{{- if not (has $network $allowed) -}}
{{- fail (printf "gatewayContractConfigMap.network must be one of: devnet, testnet, mainnet (or empty); got %q" $network) -}}
{{- end -}}
{{- $preset := dict -}}
{{- if $network -}}
{{- $preset = .Files.Get (printf "configs/gateway-%s.yaml" $network) | fromYaml -}}
{{- end -}}
{{- $merged := mergeOverwrite (deepCopy $preset) ($cm.addresses | default dict) -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $cm.name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ $cm.name }}
data:
  {{- range $k, $v := $merged }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
{{- end -}}
